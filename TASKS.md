# Rimward — Master Task List v1.0

Complete the build-out of *Spacer Quest: Rimward* per `docs/PRD-REIMAGINED.md` and
`docs/TECH-STACK.md`, sized for the orchestrator loop. This list supersedes
`TASKS_v0.1.md` (completed history, M0–M3 all DONE) and is shaped by
`TASKS_v0.1_Audit.md` (findings of record, 2026-07-12): the v0.1 build shipped
features that were unreachable, state that nothing read, and physics that never
bound. Every task below carries acceptance criteria that prove reachability and
consumption, not just existence.

## Orchestrator protocol

1. **Check out** the first task with `status: TODO` whose `after:` tasks are all DONE. Set it `IN-PROGRESS`.
2. **Plan** — delegate to the Fable planner with: the task block below, plus pointers to `docs/PRD-REIMAGINED.md`, `docs/TECH-STACK.md`, `TASKS_v0.1_Audit.md`, and the files named in the task. Nothing else.
3. **Code** — delegate to the coder named in `coder:` (Opus by default; Fable where flagged — those tasks have cross-cutting design decisions baked in).
4. **Review** — delegate to the Sonnet reviewer with the diff and the task's acceptance criteria. Criteria are written to be mechanically checkable AND to prove integration (reachable by a player; consumed by a named reader).
5. On pass: run the gate, commit as `T-1NNN: <title>`, set `status: DONE`, update this file in the same commit. On fail: one fix round with the coder, then escalate to Fable if still failing.

**App.tsx serialization rule** (lesson of v0.1's T-304/T-307 collision): tasks that touch `packages/ui/src/App.tsx`, `store.ts`, or `format.ts` must never be concurrently IN-PROGRESS — commit each before starting the next. The M14 chain encodes this as a strict `after:` sequence; do not parallelize it.

**Foundation reference:** the `foundation/` directory was removed from the tree in commit `afc0c771`. The 1991 rules of record live at git ref **`f2f95fa9`** — consult them as `git show f2f95fa9:foundation/rules/<file>` (e.g. `travel.ts`, `upgrades.ts`, `combat.ts`, `constants.ts`). The writing voice guide is `git show f2f95fa9:foundation/lore/User-Manual.md`.

**Gate (every task):** `npx tsc -b` clean at root (including `packages/ui/e2e/`) + `npm run lint` (covering e2e specs) + `npm run format:check` + `npm test` all green. UI tasks additionally require `npm run test:e2e -w @spacerquest/ui` green locally. Per global rules: UX-facing verification goes through the real UI, never the engine directly.

**CI-evidence rule:** review and gate run on the uncommitted diff, so acceptance criteria must be locally checkable — the local equivalent of what CI runs satisfies review. Where a criterion names CI or push-dependent evidence, it is confirmed **after** the commit is pushed and recorded in the task's Delivered note; the CI Playwright job (added by T-1001) must be green on the branch's most recent pushed commit before the next task starts.

**Standing constraints** (reviewer enforces on every task):
1. Engine stays pure: no DOM, no I/O, no `Math.random`/`Date` — all randomness through `SeededRng`.
2. Every feature reachable headlessly; UI is a client, never the owner, of a rule.
3. All state changes emit typed `GameEvent`s; state survives JSON round-trip. Any task that adds a `GameState` field ships a save migration and a round-trip test in the same commit.
4. Content is data in `packages/content`, never logic.
5. The PRD-REIMAGINED experience wins over foundation numbers. Foundation (`f2f95fa9`) is the reference of record, consulted first; every divergence is commented at the definition site with the PRD rationale. Undocumented divergence is a review failure.
6. A task is not DONE until the feature is **reachable by a player through the UI**. Engine-only tasks must name, in their block, the task that surfaces them — and that task inherits the reachability obligation.
7. Every state field, flag, or event a task adds must **name its reader**, and the acceptance must assert the reader consumes it. If nothing reads it, it isn't a feature — it's a receipt.

**Rebalance fallout rule:** a task whose change breaks an existing green test fixes that test **in the same commit** — leaving it for later is a review failure. Balance thresholds are staged deliberately: interim loose bands (e.g. trader clears ≥50% of seeds) hold until T-1603 asserts the canonical targets; earlier tasks must not enshrine numbers T-1603 will move.

Statuses: `TODO` | `IN-PROGRESS` | `DONE` | `BLOCKED(reason)`

---

## M10 — Make the gate real

### T-1001 · CI runs the whole gate — `status: DONE` · `coder: opus` · `after: —`
The v0.1 gate was fiction: `packages/ui` has a `test:e2e` script but no `test` script, so `npm test --workspaces --if-present` silently skips all 11 Playwright spec files, and `ci.yml` has no Playwright step — every M3 task shipped DONE under an unenforced gate. Add a CI Playwright job (`npx playwright install --with-deps chromium` + build + `test:e2e`); bring `packages/ui/e2e/` under `tsc -b` (tsconfig `include`) and under eslint (widen the `files` glob); fix whatever latent type/lint errors that surfaces.
**Accept:** all 11 existing spec files pass locally via the exact command the new CI job runs (`npm run test:e2e` in `packages/ui`); a deliberately broken spec fails that command locally (demonstrated and reverted, recorded in the Delivered note); a type error injected into an e2e spec fails `npx tsc -b` locally; post-push, the branch's CI run shows the Playwright e2e job green (recorded in the Delivered note).
**Delivered (2026-07-12):** A second CI job `e2e` ("Playwright e2e", node 22 matching the build job, 20-min timeout) checks out, builds via `npx tsc -b`, installs chromium with `--with-deps`, and runs `npm run test:e2e` in `packages/ui`. The e2e specs are now inside both static nets: a new composite `packages/ui/e2e/tsconfig.json` (DOM libs, references engine+content, includes `playwright.config.ts`) is referenced from the root tsconfig, and the eslint `files` glob gained `packages/ui/e2e/**/*.ts`; the one latent error that surfaced (`unbound-method` on the native value-setter extraction in `sound.spec.ts`) is fixed with a justified inline suppression rather than ignored. Demonstrations performed and reverted: a deliberately broken smoke spec fails the exact CI command (1 failed / 37 passed), and an injected e2e type error fails `npx tsc -b` with TS2322. All 37 specs pass locally in 8.5s. Post-push CI verification: run 29202483103 on commit 95907736 — both jobs green, "Playwright e2e" executed all 37 specs (37 passed, 22.2s in CI). **Scope note:** the review-vs-CI ordering contradiction this task exposed (CI evidence cannot exist pre-commit) is fixed in the header's new CI-evidence rule, amended in this commit. **Post-review fix (2026-07-13):** the e2e job now caches the Playwright chromium download (`actions/cache` on `~/.cache/ms-playwright`, keyed on the lockfile) — previously every CI run re-downloaded the browser.

### T-1002 · Deep schema-drift guard + seed in the save envelope — `status: DONE` · `coder: opus` · `after: —`
`loadSave` silently deletes any nested state field the Zod schema doesn't know (verified live: `player.reputation` → `undefined` after round-trip); the T-115 compile-time drift guard covers top-level `GameState` keys only. Extend drift protection to nested objects — recursive compile-time `keyof` guards over `PlayerState`/`ShipState`/`NpcState`/etc., or a property test that injects unknown nested keys into fixtures and asserts a loud failure instead of a silent strip. Also move the RNG seed from localStorage-only (`sq.save.seed`) into the versioned save envelope with a migration — TECH-STACK's "reproducible bug reports" non-negotiable. This task is the named prerequisite for T-1503 (faction rep) and for every M12/M13 task that adds a `GameState` field.
**Accept:** a regression test proves an unknown nested field under `player.` fails loudly on load rather than silently stripping; the seed survives save→load→save byte-identically and lives in the envelope; the version-bump migration fixture is green.
**Delivered (2026-07-12):** Every engine-owned nested schema in `schema.ts` (`PlayerState`, `ShipState`, `ComponentState`, `NpcState`, `MarketState`, `CargoContract`, `ChartsState`, `DiscoveredPoi`, `LegacyState`, `NemesisFileState`, `SignalFragmentRecord`, `DeedRegistryState`, `EarnedDeedState`, `StoryletState`/`StoryletScheduleState`, `EncounterState`/`EncounterInterceptorState`, `EraEventState`, `DawnHand`, `PendingTravelState`, `CheckResult`, and the `GameState` root) is now `.strict()`, so an unknown key at any nesting depth throws a `ZodError` on load instead of being silently dropped — the exact `player.reputation` bug is now a regression test (`schema.test.ts`, three new cases: shallow-nested, deeply-nested under `player.ship`, and top-level, plus a fourth proving a clean state still round-trips unchanged). The compile-time `keyof` guard (T-115) is extended in lockstep from top-level-only to one `AssertEqual` assertion per engine-owned interface/schema pair, so a field added to an interface without a matching schema entry now fails `tsc`, not just runtime. Content-shaped structures (storylet offers/requirements) are a named, commented exception left in Zod's default strip mode, since content legitimately owns fields the engine schema doesn't model. Separately, the RNG seed moved out of the UI-only `sq.save.seed` localStorage key into the versioned save envelope: `SaveEnvelopeSchema` gained an optional `seed` field, `CURRENT_SAVE_VERSION` bumped 1→2 with an identity 1→2 state migration (the GameState shape didn't change, only the envelope did), and `createSave`/`loadSave` now take/return the seed alongside state (`LoadedSave`). A pre-v2 save has no recorded seed and loads with `UNKNOWN_LEGACY_SEED` (0); `packages/ui/src/store.ts` treats that sentinel as a signal to fall back to the legacy `sq.save.seed` key for one read, after which the next autosave self-heals the save to v2 with the seed embedded. New `save.test.ts` coverage proves save→load→save is byte-identical across the seed, that an explicit seed of 0 survives distinctly from the sentinel, and that a hand-built seedless v1 envelope migrates green and backfills the sentinel. Full verification: all 228 engine + 30 sim tests green, root `tsc -b` clean, `eslint .` clean, and all 37 Playwright e2e specs green (`packages/ui/e2e/onboarding.spec.ts` and `wire.spec.ts` updated to pass a seed into `createSave` at their call sites). **Scope note:** the content-schema strip-mode exception (storylet offers/requirements) is deliberate, not a gap — those structures are content-owned and adding `.strict()` there would reject legitimate story content the engine schema doesn't fully model. **Post-review fixes (2026-07-13):** two claims above were inaccurate and are corrected in code, not just in prose. (1) "An explicit seed of 0 survives distinctly from the sentinel" was false — `UNKNOWN_LEGACY_SEED` was itself 0, so a seed-0 save and a migrated seedless v1 save were indistinguishable; the sentinel is removed and `LoadedSave.seed` is now `number | null` (null = pre-v2 seedless save), with the UI fallback keyed off null and a regression test asserting seed 0 vs null are distinct. (2) The storylet content shapes were not the only strip-mode exception: the entire `GameEvent` union (43 variants, part of `GameState.eventLog`) was in runtime strip mode with no drift guards — the exact silently-stripped-field bug class this task exists to stop; all 43 variants now carry compile-time `AssertEqual` keyof guards plus a discriminator-set guard (a mutation probe confirmed they fail `tsc`), with runtime strip mode retained deliberately for forward-compat and the boundary comments updated to disclose both exceptions.

### T-1003 · Typed fails for Explore + UGT protocol honesty — `status: DONE` · `coder: opus` · `after: —`
`actions/exploration.ts` throws raw `Error`s on three type-valid player inputs (no die, spent die, bad index), violating the typed-fail-event convention and crashing the UGT adapter. Convert them to typed fail events. In the protocol layer: surface `ActionBlocked` events in the session eventLog instead of discarding them (UI/protocol parity — today the two event streams diverge); add `deserializeSession` coverage (currently zero — the "backbone of deterministic replay" is untested); replace the tautological replay test with a replay against a committed golden fixture covering every action type.
**Accept:** all three bad Explore inputs driven through the UGT adapter emit typed fail events with no crash; a serialize→deserialize→resume test asserts byte-identical continuation; the replay test asserts against a committed fixture, not the function under test.
**Delivered (2026-07-12):** `resolveExploration` now resolves all three malformed-but-type-valid inputs to typed `ExplorationFailed` events with new reasons (`no-die`, `invalid-die-index`, `die-already-spent`) plus a wire line each — no die spent, no fuel burned, no throw; `types.ts`/`schema.ts` carry the widened reason union. The protocol layer commits `ActionBlocked` result state instead of discarding it, so a UGT-driven session's eventLog now matches a UI-driven one. `deserializeSession` gains real coverage: a serialize→deserialize→resume test asserts byte-identical continuation, and the replay test now replays a committed golden fixture (`packages/sim/src/__tests__/fixtures/`) covering every action type rather than comparing the pure function to itself. `PROTOCOL.md` updated to match. Suites grew 220→238 (engine) and 30→37 (sim), all green with clean tsc/lint/format. **Recovery note:** review and gate passed inside the orchestrator run; the run halted at `commit-not-verified` because the commit-stage agent returned while its background `npm test` was still running — the gate was re-run and verified by the operator before this commit. **Post-review fixes (2026-07-13):** "covering every action type" held only at top-level granularity — the replay golden never exercised Shipyard `buy-component-tier`/`buy-special-equipment` or Combat `run`/`fight` stances, and the coverage guard was a hardcoded list a new action type would not trip. The fixture logs now cover all 7 action types, all 4 Trade sub-actions, all 4 Shipyard kinds, and all 3 combat stances (goldens regenerated via the committed `gen-golden.ts`; a perturbed-log mutation check confirmed the golden is real), and the guard is exhaustive by construction (`satisfies Record<PlayerAction['type'], true>` tables) so a new discriminant fails `tsc`. PROTOCOL.md now documents the typed `ExplorationFailed` reasons and its Malformed example no longer cites the exact input Explore handles as a typed event. Count correction: the engine baseline before this task was 228 (per T-1002's note), not 220.

### T-1004 · Test & metric honesty pass — `status: DONE` · `coder: opus` · `after: —`
Fix the vacuous tests the audit flagged before any rebalancing lands, or the rebalance goes unmeasured. The batch-vs-stepped day-equivalence test compares `advanceDay` against its own body — rewrite it against a committed golden fixture (hash of final state + event log for a fixed seed/script). The 500-seed tier-band property asserts a clamp three lines above the selection — rewrite it to test matchmaking (kind bands, non-degeneracy: named NPCs are ever selected, all tiers appear). Redefine `fuelStarvationDays` in the sim report from `fuel === 0` (has fired 0 times in 6,000 simulated days) to "days the player cannot afford the cheapest available jump."
**Accept:** the Delivered note demonstrates each rewritten test fails under a seeded mutation of the rule it guards (mutation applied, observed red, reverted); a scripted broke-and-dry sim registers `fuelStarvationDays > 0`; the golden fixture is committed.
**Delivered (2026-07-12):** Three vacuous tests are now real. (1) **Day equivalence → committed golden.** The batch-vs-stepped test compared `advanceDay` against a hand-inlined copy of its own body — a tautology that moves both sides together under any rule change. It is replaced by `day.test.ts`'s "matches the committed golden for a 10-day scripted batch advance" (and a sibling Storylet golden), which pins the final-state hash and the day-event-stream hash for a fixed seed/script to committed constants in `packages/engine/src/__tests__/fixtures/day-loop-golden.ts` (regenerable via the committed `gen-day-loop-golden.ts`). (2) **Tier-band clamp → matchmaking.** The 500-seed property only re-asserted the `player.tier ±1` clamp computed three lines above the pick. `encounter.test.ts`'s "matchmaking is route-correct and non-degenerate across a 500-seed sweep" now drives real matchmaking across core/rim/andromeda routes: anonymous interceptor kinds must lie in the route's allowed set (core excludes REPTILOID/RIM_PIRATE; andromeda is REPTILOID-only), BOTH named and anonymous pools must be reached (non-degeneracy), and all five tiers must surface. (3) **`fuelStarvationDays` redefined.** The sim report's `fuel === 0` count (0 hits in 6,000 days — every policy tops the tank) becomes the exported `cannotAffordCheapestJump(state)`: stranded = even after spending every credit on fuel, `maxReachableFuel < cheapestJumpFuel` for the nearest reachable system, priced with the same `jumpFuelCost` the engine charges. `campaign.test.ts` adds a unit test (stranded credits=0/fuel=5 → true; full tank → false; dry-but-flush → false) and a scripted broke-and-dry campaign that dumps all credits into debt then hops the nearest lane every day, registering `fuelStarvationDays > 0`. **Mutation/red/revert evidence, each observed locally:** (a) `haggleDc 12→8` in `actions/trade.ts` → day golden red (`day.test.ts` 1 failed/7 passed, hash mismatch at the stateHash assertion); reverted → 8/8 green. (b) reverting `cannotAffordCheapestJump` to the old `state.player.ship.fuel === 0` → both new fuel tests red (unit "distinguishes stranded from merely low" and the broke-and-dry sim asserting `fuelStarvationDays > 0`, seen as `expected 0 to be greater than 0`); reverted → 2/2 green. (c) adding `'PIRATE'` to the andromeda branch of `allowedAnonymousKinds` in `actions/travel.ts` → matchmaking sweep red at `expect(route.allowed).toContain(interceptor.kind)`; reverted → green. Gate clean: `npx tsc -b` clean, `npm run lint` clean, `npm run format:check` clean (the two long-line files were re-wrapped with prettier), full engine + sim suites green. The golden fixtures (`day-loop-golden.ts`, `gen-day-loop-golden.ts`) are committed alongside this change. **No standing-constraint obligations triggered:** no new `GameState` field (so no save migration/round-trip), no `Math.random`/`Date`/DOM in touched engine files, no foundation-divergence numbers altered.

### T-1005 · PRD & doc corrections — `status: DONE` · `coder: fable` · `after: —`
Bring the docs and the code's comments back into truth. Amend the PRD upward where the build is better than the spec: legacy/succession detail into §5.2 (cargo forfeiture, scheduled-storylet cancellation, the hand dying with the ship); contract competition as a named mechanic; the anti-poverty-trap principle (debt as ledger, never negative balance) stated as design law; full-screen combat/resolution overlays blessed in §4's "one screen" language. Correct the record: the `shipyard.ts:96` "intentional divergence" comment is false — foundation **does** cap hull-scaled prices at 20,000 (`git show f2f95fa9:foundation/rules/upgrades.ts`, ~line 731); add a v0.1 errata note covering that, the T-101 "payments shifted" claim, the T-106 "shared job pool" description, and the T-201 poverty-criterion scoping. Record the `f2f95fa9` foundation ref and the PRD-over-foundation balance policy in `docs/`.
**Accept:** PRD sections amended; the false divergence comment corrected at its definition site; errata section committed; zero behavioral diffs (`npm test` untouched-green).
**Delivered (2026-07-12):** PRD-REIMAGINED.md amended in four places: §2 goals gained "Contract Competition" as a named mechanic (rivals claiming jobs off the player's own manifest board, not a shared pool) and a new "Scarcity of choices, never a poverty trap" design-law bullet (debt as ledger, never a negative balance, income floors for NPCs); §4's "one screen" principle now explicitly blesses full-screen combat/resolution mode-takeovers as diegetic, not a second UI; §5.2's death/legacy paragraph gained the cargo-forfeiture, scheduled-storylet-cancellation, and same-day-succession detail that was already true of the build but missing from the spec. The false "intentional engine divergence" comment on the hull-scaled equipment price cap was corrected at its definition site in `packages/engine/src/actions/shipyard.ts` (and the companion note in `shipyard.test.ts`): foundation (`f2f95fa9:foundation/rules/upgrades.ts` ~L731) does cap at 20,000, so `Math.min(hull.strength * 1000, 20000)` is faithful, not a divergence. `docs/BALANCE-POLICY.md` is new: Part A names the `f2f95fa9` foundation reference of record and marks `legacy/` as history rather than balance authority; Part B states the PRD-over-foundation precedence and the no-false-divergence-comment rule; Part C is a v0.1 errata log with four entries (E1 the hull-price-cap non-divergence above, E2 T-101's false "payments shifted" claim, E3 T-106's inaccurate "shared job pool" description corrected to same-manifest-board claiming, E4 T-201's poverty-trap criterion scoped down to the three competent NPC policies rather than every policy). **Scope boundary:** doc/comment-only change — no engine logic, prices, or DCs were touched, so this delivers zero behavioral diff by construction; `npm test` was re-run and confirmed all-green rather than merely asserted. **Post-review addendum (2026-07-13):** the one remaining unverified divergence claim of the same class — `shipyard.test.ts`'s "T-105 intentionally diverges from foundation strip-and-proceed side effects" — was verified against `f2f95fa9:foundation/rules/upgrades.ts` and is TRUE in substance (foundation strips TITANIUM_HULL↔AUTO_REPAIR and CLOAKER-under-ARCH_ANGEL/STAR_BUSTER and proceeds; the engine hard-fails uniformly) but overbroad: three of the eight conflict rows (CLOAKER↔AUTO_REPAIR both ways, CLOAKER over ARCH_ANGEL) hard-fail in foundation too. Both comment sites now carry the verified reference and direction breakdown; no errata entry needed.

---

## M11 — The physical galaxy

### T-1101 · Real 2D starmap + destination gating — `status: DONE` · `coder: opus` · `after: T-1004`
All 20 playable systems sit at `y=0, x=id-1` in `content/systems.ts`, so `calculateDistance` degenerates to the `|id difference|` it was chartered to replace — and NEMESIS sits at `(0,0)`, the same point as Sun-3, one jump from home. Author real 2D coordinates for systems 1–20 (core cluster with genuine route choice; rim outlying per PRD §9 geography); move NEMESIS to a remote point. Add engine-side destination gating: travel to NEMESIS/Andromeda/special systems emits a typed fail (unlockable later via the flag T-1505 sets) — today `resolveTravel` accepts any destination and the UGT protocol offers all 28. Re-home the Reptiloid spawn tables off Andromeda-only routes (they currently spawn only where §10 forbids travel).
**Accept:** no two systems share coordinates (content test); rim systems' mean distance-from-core exceeds core–core mean distance; `Travel` to NEMESIS via `applyPlayerAction` emits a typed fail, not success or throw; Reptiloids reachable in an encounter seed sweep; the starmap Playwright spec re-fixtured and green on the 2D spread.

**Delivered (2026-07-12):** Authored genuine 2D coordinates for all 20 core/rim systems in `content/systems.ts` (non-collinear core cluster, rim shell ~20–24 units out) and relocated NEMESIS off Sun-3's `(0,0)` to a remote point beyond the fuel ring; added `isGatedDestination`/`GATED_DESTINATION_MIN_ID` and an engine-side gate in `day.ts` `applyPlayerAction` that emits a typed `ActionBlocked` (`reason: 'destination-locked'`) for Andromeda/special-system travel until the `nemesis.crossing.unlocked` flag (T-1505) lifts it; mirrored the gate in the sim's policy pickers (`travelableSystemIds`) and the UGT protocol's `legalActions` so no client is ever offered a destination the engine will refuse; re-homed Reptiloid spawns onto the now-reachable rim frontier (`allowedAnonymousKinds`) since the old Andromeda-only spawn was unreachable once sealed. Fallout absorbed in-task: re-derived golden hashes, distance-priced fixtures (economy, onboarding), and re-seeded Playwright specs (starmap, combat, onboarding) against the new coordinate spread; Andromeda systems (21–26) themselves remain out of scope per PRD §10 and stay sealed behind the gate, not re-geographied.

### T-1102 · Fuel scarcity overhaul — `status: DONE` · `coder: fable` · `after: T-1101`
"Fuel is the plot" (PRD §4, differentiator 3) is inert: `jumpFuelCost` caps at `min(ty,100)/2` so every jump of distance ≥8 costs exactly 50 fuel, and `maxFuel` is a hardcoded 10,000 never derived from the hull. Remove the cap (per-distance cost that keeps rising); derive `maxFuel` from hull via foundation's `calculateFuelCapacity` (`git show f2f95fa9:foundation/rules/travel.ts`); put the starter tank on the PRD §7.1 scale ("two jumps costs 240 units; you're carrying 300"); reprice depots to match; retune the sim policies' fuel planning in-scope. This sets the game's central scarcity curve — every divergence from foundation commented with PRD rationale per constraint 5. Existing fuel-dependent tests and the starmap fuel-ring spec are this task's fallout to fix.
**Accept:** a test reproduces the §7.1 scenario (a two-jump route costs ≈240 units against ≈300 starter capacity); a cross-map jump is unaffordable on a starter tank (typed fail); a hull upgrade raises `maxFuel` (A/B test); the trader policy still clears Tour One on ≥50% of 50 seeds after in-task retuning (interim band — final band is T-1603's); the starmap fuel-ring Playwright spec is green against the new engine math.

**Delivered (2026-07-12):** Replaced `jumpFuelCost`'s capped `min(ty,100)/2` packaging with a strictly per-distance `perUnit × distance` (no ceiling — the exact defect fix, since the old cap flattened every jump ≥8 distance to a constant 50 and made distance stop mattering); added `calculateFuelCapacity(hullStrength, hullCondition)` (ported from foundation's formula with a divergence: multiplier 30 instead of foundation's 10, chosen to land the starter tank at PRD §7.1's ~300 rather than foundation's ~100) and wired it through a single `syncMaxFuel` chokepoint called once per `applyPlayerAction` (day.ts) and once on save load (`deserializeState`, migrating legacy flat-10000 saves and clamping current fuel to the new ceiling). Repriced `RIM_FUEL_BUY_PRICE` 25→8/unit so a rim jump's credit cost stays near its pre-change value under the new fuel-burn math. Retuned both sim policies (trader and veteran) to choose the destination before sizing the day's refuel, cap signable contracts to a fraction of tank capacity, rank contracts by net (payment minus fuel cost) instead of gross payment, and raised the trader's debt-payment reserve — fixing a scarcity deadlock where a policy could strand itself on a leg it couldn't complete. Added a typed `insufficientFuel` flag to `TravelEvent` (schema + types) so a refused cross-map jump is a typed fail surfaced distinctly in the UI, not inferred from `fuelUsed === 0`. All engine (257), sim (41, incl. the 50-seed Tour One clear-rate test at 100% and the 200-day campaign solvency test), and the five fuel-touching Playwright specs (starmap, combat, manifest-trade, onboarding, settings-saves) are green against the new math. Deliberate scope boundary: the starter `hull.strength` itself was left at 1 rather than bumped to hit the 300-fuel target directly, because it's load-bearing elsewhere (cargo-pod capacity, shipyard tier gates, equipment pricing) — the fuel-capacity multiplier was tuned instead, keeping the hull-upgrade A/B monotonic without rippling into the rest of the economy. **Post-review precision (2026-07-13):** the "100%" Tour One clear rate is an authoring-time measurement recorded in a test comment; the enforced assertion is ≥60% (above the interim ≥50% band). The stale "ranked by payment, richest first" docstring on the sim contract helper was corrected to disclose the trader's downstream net re-ranking.

### T-1103 · Encounter rate & trigger repair — `status: DONE` · `coder: opus` · `after: T-1101`
Core-route encounters fire at 8% per jump vs foundation's `ENCOUNTER_BASE_CHANCE = 0.30` — an uncommented 4× cut to the game's headline mechanic — and encounters are only generated when the pilot check **succeeds**, so a botched jump is perfectly safe (backwards from fiction). Raise base encounter chance toward foundation's numbers scaled by route danger (any residual divergence commented per constraint 5), and decouple encounter generation from pilot-check success. The combat/onboarding Playwright seed fixtures are this task's fallout to re-derive.
**Accept:** a 1,000-jump seeded frequency test lands within a stated band per danger tier; a failed pilot check produces an encounter in a seeded test; combat and onboarding Playwright fixtures re-derived and green; divergence comments present at the definition sites.
**Delivered (2026-07-12):** `ROUTE_DANGER_CHANCE` re-anchored to foundation's headline numbers — tier 1 = 0.30 (restores `ENCOUNTER_BASE_CHANCE`), tier 3 = 0.40 (restores `ENCOUNTER_RIM_CHANCE`) — with tiers 2/4/5 documented as interim Rimward-only interpolation/extrapolation owned by T-1603. Encounter generation is decoupled from pilot-check success: `generateEncounter` is called once, unconditionally, with byte-identical rng on the success path; a botched jump can now be interdicted (the fiction's "drop out of the warp lane into a waiting interceptor"), with the no-new-field arrival semantics documented as a deliberate design call. New rule with named reader: `TOUR_ONE_ENCOUNTER_MULTIPLIER = 0.5` damps encounters during the TOUR_ONE era (PRD authors Tour One around "one full combat"), read by `generateEncounter` and asserted by a dedicated A/B test. Tests: the 1,000-seed per-tier frequency-band test (veteran rates), the Tour One damping test, and the failed-check interdiction test; the day-loop golden script gained its first Combat coverage (the seed-1 day-3 interdiction is fought and defeated in-script) and its hashes re-derived via the generator; five Playwright specs re-fixtured. Gate: 260 engine + 41 sim unit tests, 37/37 e2e, clean tsc/lint/format. **Recovery note:** the coder/reviewer/gate agents were killed mid-task by a session usage limit; the operator completed the remaining work (one unformatted file, the stale golden hashes/comment, the era mismatch in the frequency test, and the missing in-script fight) and re-ran the full gate before this commit. **Post-review precision (2026-07-13):** four of the five named Playwright specs were re-fixtured; `combat.spec.ts` was re-verified unchanged (its seed values confirmed still valid) rather than re-fixtured. `ROUTE_DANGER_CHANCE` lives in `packages/content/src/systems.ts` (imported by the engine), not in the engine package itself.

### T-1104 · Rim & contraband contract economy — `status: DONE` · `coder: opus` · `after: T-1102, T-1103`
`rollContract` only ever issues destinations 1–14 and cargo types 1–9, so no contract routes to the Rim, the six rim cargo types are never issued, and Contraband (type 10) is unobtainable — the game's namesake region has no payday and the smuggling pillar has no supply. Extend `rollContract` to issue destinations 1–20 and all cargo types including rim cargo 15–20 and Contraband 10 (rare, flagged, port-gated); price the rim premium against 5× fuel prices and 3× danger so "one more run to the rim" (PRD §9) is the high-stakes payday the design names as the soul of the game. Contraband contracts set the carrying state whose reader is T-1305's patrol scans.
**Accept:** a 200-seed sweep issues ≥1 contract to every rim system and every cargo type incl. Contraband; the sim trader's formerly-dead rim-hunting path executes and completes a rim delivery (asserted); rim EV premium within a stated band; a contraband contract sets the carrying state and the reader is named in code comments (T-1305).
**Delivered (2026-07-13):** `rollContract` now draws destinations from the full 1–20 range (core 1–14 the everyday board, rim 15–20 offered at a deliberately rare `RIM_DESTINATION_CHANCE = 0.12` so "one more run to the rim" stays exceptional rather than a third of every board) and builds its cargo pool from content data — core 1–9 always, plus rim goods 15–20 when the origin is a rim port; Contraband (10) is rare (`CONTRABAND_CONTRACT_CHANCE = 0.08`) and port-gated behind a new `allowsContraband` flag set on all six rim systems (`packages/content/src/systems.ts`). Payment now multiplies by a per-destination `dangerMult` (3× on the rim, 1× — a no-op — in the core) with the payment cap scaled per danger tier (`PAYMENT_CAP_PER_DANGER = 15000`) so the rim's fuel+danger premium isn't flattened by the old flat 15000 ceiling; core payments are numerically unchanged (verified: `valueMultiplier === id` for cargo 1–9 reproduces the prior formula exactly). Added `isCarryingContraband(state)` in `packages/engine/src/economy.ts`, derived from the already-serialized `activeContract.cargoType` (no new state field, no migration) — the named reader is T-1305's patrol GUILE scans. Fixed a poverty-trap the rim/contraband roll surfaced in the sim: the trader, fighter, and explorer policies (`packages/sim/src/index.ts`) previously ranked contracts richest-first without a reachability filter, so once rim destinations existed they'd sign an unfuelable/unwinnable rim run and strand; all three now filter to jump-reachable contracts before picking, with the trader additionally relaxing its re-flight-margin cap to the full tank when nothing fits inside it. Re-derived the day-loop and replay golden fixtures and the `onboarding.spec.ts` Playwright fixture (seed 424242's contract 0 now routes to system 9 instead of 3, paying 3,420cr instead of 3,120) against the new RNG draw order. **Scope boundary:** this task only sets the contraband-carrying predicate and its supply side; consuming it — patrol GUILE scans, confiscation, fencing — is T-1305's job, named explicitly as the reader in code comments and left untouched here. Gate: 263 engine + 42 sim unit tests, 4/4 onboarding e2e specs, all green.

---

## M12 — One d20 society

### T-1201 · NPCs roll real checks — `status: DONE` · `coder: fable` · `after: T-1004`
PRD §7: "the player and the galaxy run on one system — there is no separate AI." In fact `dice.ts:check()` is never called by `npc.ts`: NPC Trade/Travel/Patrol days involve no roll at all (trades always succeed and always bank the payment), and Combat/Socialize use a raw `d20` against hardcoded `≥12`/`≥14`. Route all five NPC verbs through the shared `check()` with content-defined stats and DCs, emitting `StatCheck` events. This is differentiator #1's load-bearing task; T-1202 builds on its events.
**Accept:** a test asserts every NPC action resolution emits a `StatCheck`; a 200-day sim shows a non-degenerate NPC trade failure rate (>5%, <60%); no hardcoded DC literals remain in `npc.ts` (constants sourced from content, asserted).

**Delivered (2026-07-13):** All five NPC verbs (Trade, Travel, Combat, Patrol, Socialize) now roll through the same shared `check()` the player uses, sourcing their stat affinity and DC from new content tables (`NPC_CHECK_DCS`, plus win/loss credit and fuel constants) instead of the old raw `d20` inline thresholds, and each resolution emits a `StatCheck` event tagged with a `npc-trade`/`npc-travel`/`npc-combat`/`npc-patrol`/`npc-socialize` `actionContext` so the wire and future tasks (T-1202) can read NPC rolls without string-parsing the actor. A verb⟺StatCheck invariant is pinned by a full-cast sweep test, a source-scan test proves no `>=12`/`>=14` literals remain in `npc.ts`, and a 200-day sim test confirms a non-degenerate trade failure rate (~37% at seed 1, within the required 5–60% band). Deliberate scope boundary: the Trade verb's failure carries no credit/fuel penalty by design (documented in-code) — Trade is the highest-frequency NPC verb and a skill check, so a per-trade economic penalty was measured to skew the 200-day wealth distribution degenerate; a failed Trade instead produces a distinct "soured run" wire-facing narrative and a recorded failed `StatCheck`, leaving margin-scaled economic consequences (nat-20/nat-1 stories, margin-fed payouts) to T-1202 as planned.

### T-1202 · Nat-20/nat-1 stories + margin scaling — `status: DONE` · `coder: fable` · `after: T-1201`
PRD §6: "A natural 20 or natural 1 always generates a story, and stories go out on the Galactic News Wire" — today nothing anywhere inspects nats for the wire, and `CheckResult.margin` is computed, displayed, and read by no rule. Make any `check()` nat-20/nat-1 — player or NPC — always generate a wire story (authored templates in content); make margin scale the key outcomes: haggle bonus, combat damage, tribute price. "The margin decides how well it goes" stops being decoration. The UI's `CheckBreakdown` stays a pure client (it already displays margin).
**Accept:** property test — every nat in a 300-day sim has a same-day wire entry; same-seed haggle A/B shows higher margin → strictly higher bonus; combat damage varies with margin; the PRD §6 sample wire line's template is producible in a seeded run; `CheckBreakdown` renders unchanged.
**Delivered (2026-07-13):** Every `check()` nat-20/nat-1 — player or NPC — now generates a same-day wire story: a new engine wire scanner (`packages/engine/src/wire.ts`) inspects the day's `StatCheck` events and emits `WireEntry`s from authored templates in `packages/content/src/wireStories.ts` (pure template data, a Record of string arrays — no logic), with the scanner's randomness forked from a stable pre-action/pre-dusk `rngState` so it cannot perturb persisted RNG or golden fixtures. Margin now scales the three named outcomes: haggle bonus (per-margin credit floored at 1, so higher margin → strictly higher payment — same-seed A/B test plus a monotonicity sweep), combat damage (`applyEnemyPressure` deals 3 on a nat-20, 2 at margin ≥10, else 1 — was a hardcoded 1), and tribute price (margin-scaled discount). Each divergence carries a FOUNDATION DIVERGENCE comment at its definition site citing `f2f95fa9` with the PRD rationale. The PRD §6 sample wire line is reproduced verbatim by a seeded test from `NAT_WIRE_TEMPLATES.gamble.nat20[0]`. Property test: 300-day sims at seeds 1/7/99 assert every nat StatCheck day carries at least that many wire lines, plus a WireEntry round-trip test. `CheckBreakdown` is verifiably unchanged (zero diff under `packages/ui`). No new `GameState` field, so no migration was required; day-loop and replay goldens re-derived. **Recovery note:** review and gate passed inside the orchestrator run, but the commit-stage agent returned early while its own background verification was still running (the same failure mode T-1003's note recorded) — the operator re-ran the full gate and performed this commit; the orchestrator's commit-stage prompt has been amended to forbid background runs.

### T-1203 · player.tier progression — `status: DONE` · `coder: opus` · `after: T-1004`
`player.tier` is set to 1 at creation and written nowhere else, locking encounter matchmaking to tiers 1–2 forever — 23 of the 30 named NPCs (including Rattlesnake, the PRD §7.4 set-piece) can never intercept the player. Define `tier = f(renown rank, ship class)`, updated on rank-up and succession; matchmaking bands open with it.
**Accept:** the veteran sim policy reaches tier ≥3 and is intercepted by a tier-3+ named NPC with no test setting tier manually; a roster reachability sweep shows 0 structurally-unreachable named NPCs; tier survives save round-trip with defined succession behavior.
**Delivered (2026-07-13):** `player.tier` is now a pure derivation (`packages/engine/src/tier.ts`, `computePlayerTier`/`syncPlayerTier`) of the player's renown rank and the strongest of their weapons/hull/shields fit, clamped to the 1–5 band via `max(rankTier, shipClassTier)` so the band never closes as either axis climbs; it is recomputed at every chokepoint that can move either input — dawn/action/dusk in `day.ts`, save deserialize (after `renownRank` reconstruction) and initial state creation in `state.ts`, and succession in `legacy.ts` (carried rank, reset ship, band reflects the renowned name hunting a fresh junker) — so no call site can leave a stale literal behind. Encounter matchmaking (`actions/travel.ts`) is the sole reader and needed no changes since it already read `state.player.tier`. Golden fixtures (`day-loop-golden.ts`, `replay-golden.ts`) and the sim campaign test were re-derived where the newly-opening band surfaces a different, tier-appropriate named interceptor (e.g. Smuggler Ray instead of Doc Salvage) than before the fix. Deliberate scope boundary: the formula's thresholds (rank-index/2, ship-rating breakpoints at 10/30/50/70) are tunable constants authored fresh for this task, since v0.1/foundation had no player power-tier concept at all to diverge from — documented as a FOUNDATION DIVERGENCE at the module's definition site.

### T-1204 · Disposition with teeth — `status: DONE` · `coder: fable` · `after: T-1201, T-1203`
PRD §6 "They remember" is plumbed but dead: nothing reads `npc.disposition` except the bond hook and storylet triggers, the tribute/talk DC is flat `10 + tier`, and −1/dusk decay swamps every gain (measured max +1 over 4×300 sim days; the bond hook needs +5 and has fired zero times ever). Make encounter interceptor selection weight disposition (grudges hunt you, friends pass you by); give the tribute/talk DC a disposition term — the unbuilt "this is personal" Rattlesnake beat from v0.1's T-104; rebalance decay (slower decay-toward-zero, larger event deltas); turn the bond intervention into a typed character hook (keyed to the profile's Bond, not a bare threshold) with an organically reachable trigger.
**Accept:** same-seed A/B — insulting an NPC raises both their tribute DC and their interception probability; a 300-day sim produces ≥1 bond intervention and peak |disposition| ≥5 from organic play; the decay divergence is documented at its definition site.

**Delivered (2026-07-13):** Added `packages/content/src/disposition.ts` as the tuning data module (`DISPOSITION_DELTAS`, `DISPOSITION_DECAY_INTERVAL_DAYS`, interceptor-weighting constants, `TALK_DC_PER_DISPOSITION`, and the typed `BondHook` interface), and wired three new engine readers: `travel.ts`'s `chooseWeighted` (grudges raise interception odds, favor lowers them, reordering only within the existing tier-band pool so matchmaking invariants hold), `combat.ts`'s `resolveTalk` (disposition shifts the talk/tribute DC, scoped to TALK only so run/fight goldens are untouched), and `day.ts`'s dusk decay (now every `DISPOSITION_DECAY_INTERVAL_DAYS` days instead of every dusk, keyed off `state.day % N` with no new save field). The bare inline bond-hook threshold was replaced with a per-profile `bondHook` (Admiral Stern: drive-off, DC 12; Doc Salvage: fuel-gift, DC 8, with a broadened low-fuel trigger) read in `day.ts`'s endDay. Event deltas were enlarged (tribute +3, defeat −5, playerFled +2, contractSniped −1) so a single organic event clears the Bond-hook threshold and survives the slower decay. Verified by a new organic 300-day sim test (`packages/sim/src/__tests__/campaign.test.ts`) asserting a real bond intervention fires and peak |disposition| ≥ 5, plus unit coverage in `packages/engine/src/__tests__/disposition.test.ts`. Scope boundary: the disposition DC term applies to TALK/tribute resolution only, not to run/pursuit or fight rolls — those stay T-1207's territory (opposed pursuit) and are untouched by this commit.

### T-1205 · Ship components load-bearing — `status: DONE` · `coder: opus` · `after: T-1202`
Six of eight ship components are mechanically dead: combat reads only player stats and enemy tier, so weapons and shields are cosmetic and `ComponentDamaged` on them is theatre — the project's own playtest rule ("a real player would upgrade their weapons before fighting") is violated by the rules themselves. Wire each component to a named reader: weapons→attack, shields→mitigation, navigation→pilot/explore checks, robotics→repair rate, cabin→crew capacity (the socket T-1306 fills), lifeSupport→a defined survival/succession reader — or document an explicit removal. Replace the deterministic rounds-4/12/20 damage rotation (hull needs 68 never-miss rounds to die) with seeded damage targeting that can hit hull on any round. Scripted combat tests and newly-possible sim deaths are this task's fallout.
**Accept:** A/B tests — upgraded weapons shorten time-to-kill and upgraded shields reduce damage taken; hull damageable on any round with median rounds-to-kill well under 68 (property test); a reader test exists per component; the fighter policy's win rate improves with upgrades (sim A/B).
**Delivered (2026-07-13):** Added `packages/engine/src/components.ts` and `packages/content/src/components.ts` as the single named-reader/tuning-data pair for all eight ship components: `weaponVolleyDamage` (weapons→hull removed per winning volley), `shieldMitigation` (shields→condition subtracted from incoming hits, hard-capped one below nat-20 raw damage so the hull stays killable through any upgrade), `navBonus` (navigation→PILOT check modifier, wired into `travel.ts` and `exploration.ts`), `repairRate` (robotics→condition restored per single-component shipyard repair), `crewCapacity` (cabin→berths, surfaced in the shipyard preview and UI as the T-1306 socket), and `lifeSupportCritical` (lifeSupport→dusk GRIT survival gate in `day.ts`, wired to `applySuccession` on failure). `combat.ts`'s deterministic rounds-4/12/20 damage rotation was replaced with `damageComponentForHit`, a seeded uniform pick taken only after a hit lands, making hull (and every other component) reachable on any round. Every reader is baseline-subtracted so the starting junker ship reproduces the exact pre-T-1205 numbers, keeping existing goldens valid. Verified by a reader test per component (`packages/engine/src/__tests__/components.test.ts`), a property test proving median rounds-to-kill collapses well under the old 68-round floor (`combat-property.test.ts`), and a sim A/B holding the fighter policy fixed while only toggling component upgrades on/off, showing win rate improves with upgrades (`packages/sim/src/__tests__/combat-ab.test.ts`). Scope boundary: enemy-side component wiring, opposed pursuit/retreat rolls, and tribute modifiers are explicitly out of scope — those remain T-1206 and T-1207's territory.

### T-1206 · Special equipment load-bearing — `status: DONE` · `coder: opus` · `after: T-1205, T-1103`
CLOAKER, AUTO_REPAIR, STAR_BUSTER, and ARCH_ANGEL are purchasable, renown-gated, priced, tested — and read by no rule; v0.1 spent two repair rounds making them *reachable* and never checked they *did* anything. Port the rules foundation already has: `attemptCloakDuringTravel` (cloaker lowers realized encounter/escape odds), `applyAutoRepair` (dusk condition regen), and Star-Buster/Arch-Angel combat effects — from `git show f2f95fa9:foundation/rules/upgrades.ts` and `combat.ts`.
**Accept:** per-item seeded A/B tests (cloaker lowers realized encounter rate; auto-repair recovers condition at dusk; Star-Buster/Arch-Angel each change a combat outcome); the v0.1 veteran earned-play test still green; every purchasable item now has a reader test.
**Delivered (2026-07-13):** Wired all four previously-inert special-equipment modules into the readers T-1205 already established: `hasCloaker` damps the realized encounter chance by `CLOAK_ENCOUNTER_MULTIPLIER` inside `travel.ts` `generateEncounter` (a probabilistic damping rather than foundation's outright fight-skip, since the engine's PRD wins on numbers over the ported rule); `hasAutoRepair` drives a new pure `autoRepairRegen` reader (`components.ts`) that restores condition to every fitted non-hull system at dusk in `day.ts` `endDay`, ordered after enemy dusk pressure and before the life-support survival gate so it can rescue a ship driven to `lifeSupport` 0 that same night; `hasStarBuster` adds a flat siege bonus on top of `weaponVolleyDamage`; and `hasArchAngel` raises `shieldMitigation` to a guaranteed floor, still capped below the nat-20 raw-damage value so the hull-killable invariant from T-1205 survives. Every flag defaults false and each reader is additive on top of the pre-existing baseline, so no ship without the module moves and every existing golden is untouched. Verified with a per-module reader unit test plus a seeded A/B (fixed enemy/dice, module flag toggled) proving a measurable outcome shift, in `packages/engine/src/__tests__/components.test.ts`; the accept criteria's "every purchasable item has a reader test" gate additionally caught two modules T-1206 didn't originally name — Trans-Warp drive (`economy.test.ts`) and Titanium Hull (`shipyard.test.ts`) — and closed those gaps too. Scope boundary: this task is player-ship-only; enemy-side equipment, opposed pursuit/retreat rolls, and tribute modifiers are explicitly deferred to T-1207.

### T-1207 · Tribute modifiers + opposed pursuit — `status: DONE` · `coder: opus` · `after: T-1202, T-1204`
Restore foundation's tribute modifiers (Brigand ÷2, Reptiloid ×2 — currently dropped, uncommented); resolve run/pursuit and enemy retreat as opposed PILOT rolls per PRD §7.4 ("your [14] +1 vs. his pursuit roll"), margin-fed via T-1202; an enemy nat-20 escape makes the wire — the "miracle burn at Deneb-4". The combat Playwright spec's seed fixtures are this task's fallout.
**Accept:** tribute price test per interceptor class matches the ×/÷ modifiers; run resolution emits `StatCheck`s for both actors; a seeded enemy nat-20 retreat produces the wire story; the combat Playwright spec re-fixtured and green.
**Delivered (2026-07-13):** Restored the foundation tribute class modifier (`TRIBUTE_CLASS_MULTIPLIER` in `packages/content/src/combat.ts`: Brigand ÷2, Reptiloid ×2, all other anonymous kinds and every named interceptor ×1) and threaded it through engine `tributeForRound(round, kind)`, re-capped at `TRIBUTE_MAX` and exported for T-1401's UI-pack re-export. Extracted run resolution into its own `resolveRun` in `packages/engine/src/actions/combat.ts`: the player's break-off is now an opposed PILOT roll (player d20+PILOT vs. a freshly-drawn enemy pursuit d20+PILOT), and BOTH actors emit a `StatCheck` — the player's context-less (→ `nav` wire bucket) and the enemy's pursuit tagged `actionContext: 'npc-combat'` (→ `combat` wire bucket) — replacing foundation's unconditional-success break-off with a roll that can now fail. Added a symmetric post-kill enemy retreat check (PRD §7.4): a winning fight volley that would destroy the interceptor instead rolls an opposed PILOT check with the player carrying a tuned `RETREAT_KILL_EDGE` (15) advantage, so ordinary kills still resolve `defeated` and escape is reserved for a strong enemy roll or the guaranteed nat-20 "miracle burn" — a new `retreat` wire category (`wireStories.ts`) authors that beat and its nat-1 counterpart ("folded on the spot"). Named interceptors that escape this way reuse the `defeated` disposition delta rather than a new key, since the fictional beat (ship shot apart, pilot lived) is identical. Re-fixtured the combat Playwright spec and all rng-adjacent goldens (`day-loop-golden.ts`, `replay-golden.ts`, `legacy.test.ts`, `campaign.test.ts`, `combat-ab.test.ts`) since the enemy pursuit/retreat draw shifts the rng stream on every run and every kill — this stream shift is the task's declared, accepted fallout, not a regression. Scope boundary: foundation's additional `rosterIndex > 10` tribute-doubling clause was deliberately NOT ported — in the redesign `rosterIndex` is an identity/matchmaking key, not a demand-rank signal, so the modifier stays scoped to interceptor CLASS only, per the restored comment in `combat.ts`.

---

## M13 — New systems

### T-1301 · Era transition owner — `status: DONE` · `coder: opus` · `after: T-1004`
`state.era` is permanently `'TOUR_ONE'` — nobody owns the VETERAN transition, all TOUR_ONE-gated storylets never expire (guild-pressure beats remain eligible in a day-400 career), and any future `eras:['VETERAN']` content is dead on arrival. Make the Day-30 resolution (both branches) flip era→VETERAN: TOUR_ONE content expires; the unpaid branch proceeds as VETERAN-with-debt. Unblocks all VETERAN-gated content in M15.
**Accept:** sim past day 30 shows `era === 'VETERAN'` on both branches; a TOUR_ONE-gated storylet becomes ineligible and a VETERAN-gated fixture fires; save round-trip mid-transition.
**Delivered (2026-07-14):** The Day-30 resolution in `packages/engine/src/day.ts` now flips `state.era` from `'TOUR_ONE'` to `'VETERAN'` at dusk on BOTH branches — after the T-113a Wise One day-phase hook has already had its chance to fire, so nothing is clobbered by the flip's timing. The cleared branch keeps its separate `veteran.unlocked` flag as the clean-discharge discriminator; the unpaid branch flips era too but proceeds VETERAN-with-debt, with the debt itself untouched. Added the first real `eras:['VETERAN']` content, `veteran.first-lane` in `packages/content/src/storylets.ts`, to prove the gate admits veteran storylets rather than leaving them dead on arrival, plus new coverage in `packages/engine/src/__tests__/tour-one-resolution.test.ts` asserting a TOUR_ONE-gated storylet goes ineligible and the VETERAN opener fires on both the cleared and unpaid branches, and a mid-transition round-trip through the versioned save schema. `generateEncounter` (`actions/travel.ts`) stops applying its 0.5x TOUR_ONE damp once the era flips, so `packages/sim/src/index.ts`'s veteran policy was updated to fight through interceptions with a loaded contract (rather than always running) since the full-rate encounter band otherwise marooned a fuel-poor veteran sim indefinitely. Scope boundary: this task owns only the era flip, its two readers (storylet eligibility, encounter rate), and the opener storylet proving the gate works — it does not attempt the deeper VETERAN-era content buildout (NPC chains, alliance arcs, guild-pressure teeth) that later M13/M15 tasks (T-1302, T-1309, T-1502, T-1503) are scoped to deliver.

### T-1302 · Storylet triggers: era-event, renown, fragment source — `status: DONE` · `coder: opus` · `after: T-1301`
The storylet engine cannot express PRD §8.3's authoring principle ("written so the *economy* delivers it — a contract, a price spike, a wire item"): `triggerMatches` never reads `state.eraEvent`, has no renown/deed condition, and `grantFragment` hardcodes `source: 'wise-one'` for every grant. Add era-event, renown-rank, and deed-possession trigger conditions; parameterize fragment source. Rewrite v0.1 T-401's two documented divergences onto their real triggers: plague-relief delivered by an actual plague era event; the ticking-crate attached to a real Contraband contract (T-1104).
**Accept:** an era-event-triggered fixture fires only during the active event in the afflicted region (same-seed A/B); a renown-gated fixture fires on rank-up; the two rewritten storylets are reachable in a sim sweep via their real triggers; fragments record their true sources.
**Delivered (2026-07-14):** `packages/engine/src/storylets.ts`'s `triggerMatches` (now exported for direct fixture testing) gained three new gates — `trigger.eraEvent` (pins a live `state.eraEvent` by `defId` and/or requires the ship to be inside `affectedSystemIds`), `trigger.renown` (gates on `renownRankIndex(state.player.registry.renownRank)` against a `minRank`), and `trigger.deed` (requires a matching id in `registry.earned`) — plus `StoryletEffects.fragmentSource`, so `grantFragment` stamps the Nemesis file and its `FragmentAcquired` event with the grant's true origin (`FragmentSource` in `packages/content/src/nemesis.ts`: derelict/beacon/wise-one/sage/npc) instead of always `'wise-one'`, defaulting to `'wise-one'` when omitted so the T-111b Day-30 hook is unchanged. `storyletValidation.ts` validates all three new trigger shapes and the fragment-source enum. Both v0.1 divergences named in this task were rewritten onto their real hooks in `packages/content/src/storylets.ts`: `cargo.medicinals.plague-relief` now fires only on a live `plague` era event with the ship inside the epicentre (dropping its old fixed-destination stand-in), and `cargo.ticking-crate.discovered` now rides a real Contraband (type 10) contract (T-1104) instead of an unlabelled Dilithium run, with its "open it" choice granting a `derelict`-sourced fragment as the source-honesty proof. A new `veteran.guild-recognition` storylet (renown-gated, `minRank: 'COMMANDER'`, effect-free by design per Standing Constraint 7) is the acceptance fixture for rank-up firing. Fallout tracked to its root cause rather than papered over: the ticking-crate's contract-type move shifted the seed-1 golden day-loop's RNG fork (a removed `StoryletOffered` no longer bumps `dayEventCount` ahead of a `Travel` roll), so `day-loop-golden.ts`'s day-3/4 script and both TEN_DAY hashes were regenerated with the mechanism documented inline; the T-107 route-churn campaign test was rewritten from a single pinned seed (which stopped churning under the new stream) to an 8-seed sweep asserting the churn property itself; and the poverty-trap sweep surfaced a real stranding case (a combat-shrunk fuel tank at exactly the old 0.7-fraction boundary, unable to reach the cheapest board contract) fixed at the root in `packages/sim/src/index.ts`'s `planCrippledRepair`, which now also triggers repair when a degraded tank can't reach the board's cheapest contract but a pristine one could. Scope boundary: this task owns the three trigger conditions, fragment-source parameterization, and the two named exemplar rewrites plus their honest test fallout; it does not attempt the broader VETERAN-era content buildout (NPC chains, alliance arcs, guild-pressure teeth) reserved for T-1309/T-1501/T-1504.

### T-1303 · Spacers Hangout: the place + Spacer's Dare — `status: DONE` · `coder: fable` · `after: T-1202, T-1204`
The Hangout is a core PRD verb ("Visit the Hangout", §7), the site of two sample turns (§7.3, §7.5), and the rumor-table host §8.3 names — and the player cannot go there; only NPCs `Socialize`. Add a new die-costed `VisitHangout` player action at hangout-flagged systems (Sun-3 first): **Spacer's Dare** — a wagered, opposed-GUILE game against an NPC actually present in-system (their simulated position, not a random name); a rumor-table storylet host slot; a social venue (meet/befriend/insult → disposition, feeding T-1204's now-real consequences). Surfaced by T-1404 (named surfacing task per constraint 6).
**Accept:** headless — a Dare win and loss move credits both directions and shift dealer disposition; a table nat makes the wire (via T-1202); table opponents are drawn from NPCs whose simulated position is in-system (asserted); mid-day serialization round-trip; the rumor slot renders ≥1 fact from live NPC state.
**Delivered (2026-07-14):** A new `hasHangout` flag on `StarSystem` (`packages/content/src/systems.ts`) gates the venue, set true on Sun-3 (the §7.3/§7.5 sample-turn hub and player start system, so it's reachable day 1). A new die-costed `VisitHangout` player action (`packages/engine/src/actions/hangout.ts`, dispatched from `day.ts`'s `applyPlayerAction`) resolves five venues: **dare** (opposed GUILE vs. a co-located NPC's live `NPC_PROFILES` GUILE stat, mirroring `combat.ts`'s `resolveRun` framing so both sides get well-formed `StatCheck`s; a wager clamped into `[DARE_MIN_WAGER, DARE_MAX_WAGER]` and further down to what both the player and the dealer can actually cover moves credits both directions on the same roll), **befriend** (a real GUILE check against a fixed `BEFRIEND_DC`, success-gated disposition gain), **insult** (an always-landing disposition hit, the literal PRD §7.4 Flaw seed), **meet** (a small friendly nudge plus gossip), and **rumor** (a host-slot resolver, `hangoutRumors`, that synthesizes ≥1 fact per NPC from live `lastAction`/`currentSystemId` fields, in-system NPCs listed first). All five venues route through typed `HangoutEvent` fails — no die burned — for malformed input (no die named, bad die index, an already-spent die, or an `opponentId` that isn't actually in-system per the NPC's simulated position). Both Dare outcomes shift the dealer's disposition (`DARE_WIN_DISPOSITION`/`DARE_LOSS_DISPOSITION`, sized under `DISPOSITION_DELTAS.defeat`), feeding T-1204's live interception/tribute-DC readers. A hangout gate in `day.ts` blocks `VisitHangout` at an un-flagged system with a typed `ActionBlocked{reason:'no-hangout'}` (no die spent, no RNG fork, mirroring the existing destination gate); the UGT `legalActions` (`packages/sim/src/protocol.ts`) only advertises the action at a `hasHangout` system with ≥1 in-system NPC, enumerating `opponentId` to that exact NPC set. The wire's nat-scan (`checkForNatWire`, `wire.ts`) now takes the player's `currentSystemId` so a Dare nat can name the co-located dealer, threaded through both `applyPlayerAction` and `endDay`. New engine types/schema (`HangoutEvent`, `VisitHangout` action, `venue`/`wager`/`opponentId` params) round-trip through `schema.ts`/`types.ts` with tests. All balance constants (wager band, disposition deltas, befriend DC) live as documented data in `packages/content/src/hangout.ts` per the "balance numbers are data" convention `disposition.ts` set. Scope boundary: this task ships the engine-side venue, gate, and protocol surface only — the player-facing Hangout pane, wager UI, and rumor-table rendering are T-1404's named surfacing task; Penny Wise lending (T-1304) and the authored rumor-table storylet beats (T-1501) are explicitly out of scope here.

### T-1304 · Penny Wise lending — `status: DONE` · `coder: opus` · `after: T-1303, T-1002`
PRD §7.5 names "a quiet word with Penny Wise, who lends at rates that become their own quest line" as one of the bad day's three outs; Penny Wise exists in the cast as a trader and no lending mechanic exists anywhere. Add borrowing at the Hangout: principal + per-dusk interest accrual, a repay action, and default consequences that are **read** — a disposition hit plus a collection flag consumed by encounter selection (reader named). Loan state is a new `GameState` field: migration + round-trip in this commit per constraint 3. Surfaced by T-1404.
**Accept:** borrow→10-dusk accrual→repay ledger test; default measurably shifts interception odds (A/B); loan survives save round-trip with defined legacy/succession behavior; an anti-poverty property test proves a loan can never create a stateless dead-end.
**Delivered (2026-07-14):** Two new `VisitHangout` venues, `borrow`/`repay`, land the §7.5 Penny Wise out via the existing hangout resolver (`packages/engine/src/actions/hangout.ts`): Penny Wise is the lender-of-record (`LENDER_ID = 'npc-penny-wise'`, `packages/content/src/lending.ts`), not a co-located NPC, so both venues are opponent-less and reliably available at any `hasHangout` system — `packages/sim/src/protocol.ts`'s `legalActions` now advertises `VisitHangout` at an empty Hangout (previously gated on `inSystemNpcIds.length > 0`) with `venue` narrowed to `borrow` while no loan is active or `repay` while one is. `borrow` clamps the requested principal into `[LOAN_MIN_PRINCIPAL, LOAN_MAX_PRINCIPAL]` (250–5000cr) and creates `PlayerState.loan` (new `LoanState`: `lender`, `principal`, `outstanding`, `dailyRate`, `borrowedDay`, `dueDay`, `status`); `repay` pays the requested amount (default = full balance) clamped to what's affordable and owed, clearing the loan to `null` when the balance reaches zero. `day.ts`'s `endDay` accrues `ceil(principal * LOAN_DAILY_RATE)` (5%/dusk simple interest on the original principal, never compounding) to `outstanding` each dusk while a loan is live, and flips `status` to `'defaulted'` exactly once when `day >= dueDay` unpaid — applying a one-time `LOAN_DEFAULT_DISPOSITION` (−5) hit against Penny Wise via `applyDisposition` and a wire entry. Two named readers close the loop per the accept criteria: `travel.ts`'s `generateEncounter` multiplies realized encounter chance by `COLLECTION_ENCOUNTER_MULTIPLIER` (1.5x, clamped to 1) while `loan.status === 'defaulted'` (the "collectors are looking for you" mirror of the CLOAKER damp), and the same Penny Wise disposition hit feeds `chooseWeighted`'s existing grudge-weighting, making her more likely to be the interceptor. All five failure modes (malformed die input plus `already-has-loan`/`no-loan`/`insufficient-credits`) route through a typed `LoanEvent{kind:'failed'}` — no die burned — mirroring `HangoutEvent`'s fail convention. `CURRENT_SAVE_VERSION` bumped 2→3 with a `MIGRATIONS[2]` entry backfilling `player.loan: null` on legacy saves (`save.ts`), plus the equivalent `deserializeState` backfill (`state.ts`) for the non-envelope load path; `legacy.ts`'s succession carries the loan wholesale (defaulted status included) exactly like `debt`/`debtDueDay` — no reset, since "a successor claims the license, the charts, and the debts." New coverage: `packages/engine/src/__tests__/lending.test.ts` (borrow/repay/accrual/default/fail-mode/succession/round-trip) and `packages/sim/src/__tests__/lending-property.test.ts` (the anti-poverty sweep proving a loan is always a net-positive out, never a stateless dead-end). Scope boundary: this task ships the engine-side loan mechanic, migration, and the two named readers only — Penny Wise's desk pane (borrow/repay through the UI, the interest schedule shown up front) is T-1404's named surfacing task, and the T-1601 rebalance owns tuning the interim rate/term/band constants documented as such in `lending.ts`.

### T-1305 · Contraband & smuggling loop — `status: DONE` · `coder: opus` · `after: T-1104, T-1201`
Smuggling has no downside: the derelict pod pays +300cr and sets `signal.contraband.carrying`, which nothing reads — "take it" is strictly dominant, and PRD §7.2's explicit "patrol captains roll GUILE checks against smugglers" doesn't exist. Add patrol GUILE scans against a carrying player in patrol encounters; caught → confiscation + fine + patrol/faction disposition; Smuggler Ray fences pods and contraband cargo (§7.5's third out); the fence reputation flag is read by later patrol scan DCs (and by T-1503's Rebel rep — readers named). Surfaced by T-1405.
**Accept:** carrying through a patrol encounter rolls a GUILE scan `StatCheck` (test); the caught path confiscates and fines; a fence sale pays and its flag measurably changes later patrol behavior (A/B); "take the pod" is no longer strictly dominant (100-seed EV test shows nonzero realized risk).

**Delivered (2026-07-14):** A new `applyPatrolContrabandScan` (`packages/engine/src/actions/patrol.ts`) fires only when a PATROL interceptor stops a player carrying illicit cargo — the union of a live type-10 Contraband contract and the derelict sealed pod's `signal.contraband.carrying` flag, exposed as the new `isCarryingIllicit` predicate in `packages/engine/src/economy.ts` (extending T-1104's previously-dead `isCarryingContraband` reader). The scan rolls the shared `check()` primitive (patrol GUILE vs. a DC of `10 + player GUILE`, lowered if the player already carries the `fence.ray.dealt` reputation flag), emits a `StatCheck` (`npc-patrol`) plus a new `ContrabandScan` event, and on a catch confiscates the contract and/or pod independently, levies a flat 500cr fine (credit-clamped, above the pod's +300cr reward so "take it" is no longer strictly dominant), emits `ContrabandConfiscated`, and — for a NAMED patrol only — applies a −3 disposition grudge via the existing `applyDisposition` reader (anonymous patrols have no persistent NPC to attach a grudge to; faction-wide rep is explicitly left to T-1503). Two new Smuggler Ray storylets (`packages/content/src/storylets.ts`) give PRD §7.5's "third out": fencing the sealed pod for 350cr or a live Contraband cargo run at a 300cr discount, both clearing the liability, setting the shared `fence.ray.dealt` flag (`packages/content/src/contraband.ts`, imported by the engine so content and engine can't drift on the literal), and nudging Ray's disposition. All new tuning constants (fine, fence-rep scan penalty, grudge delta, fence prices) live as content data in the new `packages/content/src/contraband.ts`, each carrying a "reader" comment naming its consumer. The scan is wired into `resolveTravel` (`packages/engine/src/actions/travel.ts`) directly after `EncounterStarted`, guarded so its one `rng.d20()` draw only fires for PATROL-plus-carrying — a combination no pre-T-1305 replay golden exercises, so no golden fixture needed regeneration. New `ContrabandScan`/`ContrabandConfiscated` event types are added to both `types.ts` and the zod `schema.ts` discriminated union with coverage assertions. Verified by a new `packages/engine/src/__tests__/patrol.test.ts` (caught/evaded paths, named-vs-anonymous grudge, fence-rep DC penalty, independent contract/pod confiscation) and an extended `storylets.test.ts` asserting the two new storylet ids load. **Scope boundary:** this task confiscates and fines but does not touch faction-wide reputation or patrol-standing systems — those are explicitly T-1503's job, and the `fence.ray.dealt` flag is set here but its Rebel-rep consequence is left unread until T-1503 as planned.

### T-1306 · Dice progression — `status: DONE` · `coder: fable` · `after: T-1205, T-1002`
PRD §7's only progression axis for the dice pillar — "ship upgrades and crew can add dice, allow one re-roll, or set a floor" — does not exist; `const handSize = 5` is hardcoded and a Day-200 veteran rolls the same hand as a Day-1 nobody. Add crew (hired into cabin capacity, the T-1205 socket) and specific equipment granting +1 die, one re-roll per day, or a roll floor; parameterize `rollDawnHand` off state; hiring/upkeep as actions (Hangout/port). Design decisions on sources and caps are in-task. New state fields: migration + round-trip in this commit. Surfaced by T-1405 (named). Dawn-hand test/spec fallout (5-dice assumptions) is this task's engine-side responsibility; T-1405 owns the UI side.
**Accept:** a state with a die-granting crew rolls 6 at dawn headlessly; the re-roll consumes its single charge deterministically and serializes mid-day; the floor clamps in a property test; a veteran sim acquires ≥1 progression source by day 150 through legal play; migration + round-trip green.
**Delivered (2026-07-14):** Three hireable crew roles (`packages/content/src/crew.ts`) span PRD §7's whole dice axis — First Officer (+1 die), Navigator (one re-roll/day), Quartermaster (roll floor 5) — each hired into a cabin berth (the T-1205 `crewCapacity` socket) via a new die-costed `Crew` action (`packages/engine/src/actions/crew.ts`, hire/dismiss) and paid a per-dusk wage in `day.ts` `endDay` (an unpayable crew walks rather than going into debt). `rollDawnHand` is now parameterized off a new pure aggregator, `dawnDiceModifiers` (`packages/engine/src/dice.ts`), which sums crew-granted extra dice (clamped to `MAX_DAWN_HAND_SIZE`), takes the MAX floor across crew, and sums re-roll charges onto the day's `dawnHand.rerollsRemaining`; a new `Reroll` action spends one charge to redraw an unspent die in place, re-applying the floor. An empty crew reproduces the exact pre-T-1306 5-die roll byte-for-byte, so every existing golden stayed valid except the serialized-state hash (the added `crew: []` / `rerollsRemaining` keys). New `PlayerState.crew` and `DawnHand.rerollsRemaining` fields got a v3→v4 save migration plus round-trip coverage, new `DiceRerolled`/`CrewEvent` types in `types.ts`/`schema.ts` with drift-guard assertions, and sim protocol support (`legalActions` advertises Reroll/Crew hire/dismiss; `StateSummary` surfaces `crew`/`crewCapacity`/`rerollsRemaining`) so a headless policy can act on the new axis. Verified per Accept: a die-granting-crew state rolls 6 headlessly, a reroll consumes its charge deterministically and round-trips mid-day, a property test proves the floor clamp, and a new sim test (`packages/sim/src/__tests__/campaign.test.ts`, "T-1306 dice progression reachable through play") drives a veteran policy that earns credits, finds a free berth, and hires a real crew member via `applyPlayerAction` well before day 150 — no hand-injected state. Scope boundary: no dedicated dice-granting SpecialEquipmentId module was added (crew alone spans all three benefits without rippling a new enum through schema/shipyard mutual-exclusion/sim); the aggregator is written equipment-extensibly so a future module is a one-line add, left to T-1601's rebalance.

### T-1307 · Ports as purchasable property — `status: DONE` · `coder: opus` · `after: T-1002, T-1104`
PRD §9 keeps "ports as purchasable property" from 1991; zero code exists. Add buying a port stake: purchase price, per-dusk income, era events in the region modulate income; ownership is read by the dusk economy, the wire, and T-1503's Warlord Confederation questline (readers named). New state field: migration + round-trip in this commit. Surfaced by T-1405.
**Accept:** buy→income accrues per dusk (test); a regional era event changes income (A/B); ownership survives save round-trip and succession; the wire announces the purchase; the purchase is reachable within a veteran sim run's earnings.

**Delivered (2026-07-14):** Added `packages/content/src/ports.ts` defining the 14 core-system port authorities (flat 25,000cr price / 300cr-per-dusk base income, INTERIM pending the T-1601 rebalance) each tagged with one of four galactic alliances, with `confederation`-tagged ports named as T-1503's future Warlord Confederation reader. The engine gained a new `actions/port.ts` with a pure `resolvePortPurchase` resolver (die-costed, at-port/purchasable/not-already-owned/affordability gated, never throws), a `portDuskIncome` accrual reader wired into `endDay` (guarded on a non-empty roster so the port-free path stays byte-identical to existing goldens), and a `quotePort` buy-preview mirroring the `quoteShipyard` pattern for T-1405's ledger pane. `era.ts` gained `eraPortIncomeMultiplier` so a live regional era event scales an owned port's income up or down, exercised by an A/B test. `PlayerState.ports` is a new persisted field: the save version bumped 4→5 with a migration backfilling `ports: []` on old saves, `deserializeState` backfills the same for the legacy JSON path, and `legacy.ts` succession carries owned ports wholesale to the successor (like debt/loan, never reset). A sim test drives a veteran through 150 days of legal play (real `Port` action, no injected state) proving the buy and subsequent dusk income both land within that horizon. Scope boundary: pricing is flat across all 14 ports and marked INTERIM/non-canonical pending T-1601's real balance curve; the UI buy-preview/ledger pane itself is explicitly deferred to T-1405, and the Confederation questline payoff is deferred to T-1503 — this task only names those readers, it does not implement them.

### T-1308 · Conqueror capstone — `status: DONE` · `coder: opus` · `after: T-1004`
PRD §5.2/§9 name "Conqueror as the career capstone"; the ladder has 9 ranks, no Conqueror, and saturates (GIGA_HERO needs 15 of 17 deeds — a competent 300-day run gets there). Add the 10th rank above GIGA_HERO with a deed threshold above the current set (headroom filled by T-1504's ≥30 deeds) and one real reader: Conqueror gates the Nemesis-crossing stake term (T-1505) plus a unique citation/wire moment.
**Accept:** the rank ladder exposes Conqueror with a citation; a test asserts it is defined-but-unreached at current deed counts (reachability proven by T-1504's sweep); rank-up emits wire + Registry entry; its reader is asserted.

**Delivered (2026-07-14):** Added `CONQUEROR` as the 10th and final renown rank in `packages/content/src/deeds.ts`, appended last (index 9) with a deed threshold of 30 — above the current 17-deed set, so it is defined-but-unreached at today's content volume. `RenownRankDefinition` gained an optional `citation` field carried only by `CONQUEROR`; the engine's `evaluateDeeds` in `packages/engine/src/deeds.ts` reads it and, when present, emits that period-voice line as the rank-up `WireEntry` in place of the generic "Registry confirms Player as …" text, so every other rank's wire output stays byte-identical. `computePlayerTier`'s formula comment and tests (`packages/engine/src/tier.ts`, `__tests__/tier.test.ts`) were updated to reflect 10 ranks/index 0..9, with `CONQUEROR` clamping to the top tier band (5) alongside `GIGA_HERO`. The Zod `RenownRankIdSchema` in `packages/engine/src/schema.ts` gained the `CONQUEROR` enum value so a Conqueror `renownRank` and any `RenownRankUp`/`DeedEarned` event carrying it survive JSON round-trip. Three new tests in `__tests__/deeds.test.ts` assert: the rank is defined-but-unreached at the current deed count (`rankForDeedCount(DEEDS.length)` still saturates at `GIGA_HERO`); a synthetic 29-deed player earning one more real deed rank-ups to `CONQUEROR` with the unique citation wire (not the generic line) plus the `RenownRankUp`/`DeedEarned` Registry entries; and a `CONQUEROR` registry state plus its rank-up event round-trip through `serializeState`/`deserializeState` intact. Scope boundary: reachability through play (an actual ≥30-deed content set and a long-veteran sim proving the climb) is deliberately deferred to T-1504, as is the Nemesis-crossing stake gate that will consume `CONQUEROR` as a prerequisite (T-1505) — this task authors the rank and its capstone wire moment only, with no stub readers added to game the signal.

### T-1309 · Guild pressure & unpaid-branch teeth — `status: DONE` · `coder: opus` · `after: T-1301`
The guild-pressure beats emit six flags with zero consumers, and the unpaid Tour One branch is entirely cosmetic — its prose claims "the interest keeps running" (debt never accrues interest) and "your name carries a flag every port clerk can see" (no such flag is read). Make the unpaid branch accrue the interest its prose claims; create the port-clerk flag and read it (worse manifest terms and/or patrol attention); give every existing guild-pressure flag a consumer or remove it.
**Accept:** the unpaid sim branch shows debt growing per dusk; flagged-vs-clean same-seed A/B shows different port treatment; a named-reader test per surviving guild flag; both branches remain playable 10+ post-resolution days (no soft-lock regression).
**Delivered (2026-07-14):** The day-30 UNPAID branch (`packages/engine/src/day.ts` endDay) now sets a `guild.debt-flagged` flag whose value is a guild-standing severity computed by the new `packages/engine/src/guild.ts` (`computeGuildStanding` sums the six previously-dead `guild.pressure.tour-one.*` beat flags from `packages/content/src/guild.ts`'s signed weight table into a cooperative/hostile score; `guildSeverity` maps it to a clamped [0.5, 2] magnitude). That flag has two real readers — `economy.ts rollContract`/`generateManifestBoard` scales every board contract's payment down via `guildManifestPenalty` (threaded from `day.ts startDay`), and `actions/travel.ts generateEncounter` scales the interdiction chance up via `guildEncounterMultiplier` — both guarded so a clean, unflagged captain's rng stream and payment numbers stay byte-identical to every existing golden. The debt itself now accrues real per-dusk interest (`GUILD_DEBT_DAILY_RATE`, compounding on the outstanding balance, gated on `guild.debt-flagged` set AND `day > 30` so the day-30 resolution pass itself is untouched) and is surfaced via a new WireEntry each dusk it grows; the CLEARED branch's sign-off wire also now reads the same guild standing to pick a warm vs. cold close, giving that branch a consumer of the flags too even though it never sets `guild.debt-flagged`. New coverage: `packages/engine/src/__tests__/guild-pressure.test.ts` asserts a named reader per surviving flag plus the standing-to-magnitude chain; `tour-one-resolution.test.ts` gained a cleared-branch mirror proving debt/flag stay inert there; `packages/sim/src/__tests__/campaign.test.ts` asserts the idle policy's debt curve is flat through day 30 then strictly grows every subsequent dusk with credits never going negative (no soft-lock). Scope boundary: this task owns the six pressure flags' consumers, the interest accrual, and the port-clerk flag's two readers; it does not touch the pressure beats' own trigger/authoring (storylets.ts, already shipped pre-T-1309) or the M15 alliance/NPC-chain content those beats feed into, and the tuning constants in `packages/content/src/guild.ts` are explicitly interim, owned by the later T-1601 rebalance pass.

### T-1310 · Wise One reachability + fragment decode paths — `status: DONE` · `coder: opus` · `after: T-1302, T-1104`
The arc-opening Wise One hook triggers on `day === 30` AND being at Polaris-1 — a rim system no contract routes to, a DC-16 jump away, on the exact day the 25,000cr marker falls due — and it is the sole source of `frag-nemesis-01`, on which the game's only decode storylet gates; miss it once and the Nemesis arc closes forever. Replace it with economy-delivered design per PRD §8.3: a wire rumor plus a manifest contract routing to Polaris-1 (T-1104 makes rim contracts real), hook eligible in a window (day ≥25, on visit, never expires). Author Sage decode storylets for fragments 02–05 (today only 01 decodes — everything found by exploring is permanently stuck undecoded).
**Accept:** the explorer sim opens the arc by day 80 on ≥80% of 50 seeds with no scripted teleport; each fragment 01–05 has a decode path exercised in tests; a late visit (day 60+) still opens the arc; the delivering contract/wire item appears in a seed sweep.
**Delivered (2026-07-14):** The Wise One of Polaris-1 hook (`packages/content/src/storylets.ts` `wise-one.polaris.signal-hook`) drops its `eras: ['TOUR_ONE']` + `day: {equals: 30}` knife-edge for a window (`day: {gte: 25}`, `repeat: 'never'`), so it fires on the first Polaris-1 visit on or after day 25 whether that's Tour One or veteran era. A new economy-delivered lead, `wire.rimward.polaris-signal`, is a Galactic-Wire rumor (day ≥25, no system/era gate) whose "chase the rumor" choice drops a same-day Polaris-1 manifest contract onto the board via `addManifestContract`. `explorerPolicy` (`packages/sim/src/index.ts`) is reworked to actually reach the hook through legal play only: it resolves offered storylets first, invests in a tier-3 drives upgrade early (near-free given the strength-10 trade-in, and the structural fix that keeps fuel cheap enough to survive the rim hop), refuels before travel instead of after (closing a stranding order-of-operations bug), bounds contract-signing by the fuel actually available post-refuel, banks net-positive runs during arc pursuit instead of exploring away its credits, and once drives are ready and the 500cr fragment price is affordable, flies straight to Polaris-1 by direct Travel. Four new Sage-of-Mizar-9 decode storylets (`sage.mizar.decode-02` through `-05`) give fragments 02–05 their missing decode path, modelled on the existing `sage.mizar.decode-first`, each gated on `nemesis.hasUndecodedFragmentId` and resolving via `decodeFragment` with no parallel state flag. New coverage in `packages/sim/src/__tests__/campaign.test.ts` sweeps 50 seeds through the real `startDay`/`applyPlayerAction`/`endDay` loop (no scripted teleport) and asserts the arc opens by day 80 on ≥80% of seeds (measured 49/50, every opener by day 31), plus a second sweep confirming both the wire rumor and a natural dest-17 board contract are actually offered. Scope boundary: this task does not touch the T-1505 endgame/crossing content, the T-1601 policy-fleet rebalance of the drives-upgrade heuristic now living in `explorerPolicy`, or the Tour-One-era pressure/debt mechanics shipped under T-1309 — it only makes the arc's single entry point and its fragment decode paths reachable.

---

## M14 — UI: honest instruments

The App.tsx chain (T-1402 → T-1403 → T-1404 → T-1405 → T-1406 → T-1407) is strictly serialized — each a single commit before the next begins. All panes are pure clients: every displayed number reads an engine export or quote.

### T-1401 · Engine export pack for UI truth — `status: TODO` · `coder: opus` · `after: T-1207, T-1302`
The audit found 11 UI-owned rules — formulas `format.ts`/`App.tsx` reimplement because the engine keeps them private. Export the truth: `componentTierForStrength` (kills the UI's invented `ceil(strength/10)` inverse that makes tier-1 unbuyable), `tributeForRound` (with T-1207's modifiers), `nextRankFor`, `quoteStoryletChoice` (mirroring the blessed `quoteShipyard` pattern), a typed `WireEntry.kind` at the source (kills string-suffix flaw detection), travel preview in real units (kills the fabricated "jumps" count), and a fuel-affordability quote (the engine charges for fuel it clamps away — warn before commit). No behavior changes; T-1402 is the named consumer.
**Accept:** unit tests per export; `WireEntry` carries `kind`; the sim stats fixture is byte-identical (no behavior change); T-1402 named as consumer in code comments.

### T-1402 · UI de-rule pass — `status: TODO` · `coder: opus` · `after: T-1401`
Replace all 11 UI-owned rules in `format.ts`/`App.tsx` with the T-1401 exports. Remove the phantom TRADE-check display on contract signing (the engine spends the die and never reads its value — the instrument currently lies about the dice, the inverse of PRD differentiator 2); render signing as a die cost. Make tier-1 component purchases buyable, and fix the tier-1-hull→0-pods trap at its true owner (engine or content) in-task. Surface the fuel-overspend warning pre-commit. Fix `shipyard.spec.ts:48`, which currently bakes the tier-1 bug in as expected behavior. Wire/log spec fallout from `WireEntry.kind` is this task's to fix.
**Accept:** Playwright buys a tier-1 component (25cr) through the UI without the pods regression; the manifest sign flow renders no check; storylet locks and rank display asserted against the imported engine exports; a guard test proves `format.ts` imports rather than reimplements the listed formulas.

### T-1403 · Exploration UI — `status: TODO` · `coder: opus` · `after: T-1402, T-1003`
The audit's single biggest gap: the engine's `Explore` action — nav check, 80 fuel, beacons, derelicts, salvage, contraband pods, Signal Fragments, all built and tested since T-111 — has no UI affordance whatsoever; the Nemesis File pane can only ever render its empty state. Add an "off-lane sweep" control on the starmap pane; an `explore()` verb in the store (the missing action); the POI resolution flow (beacon/derelict); salvage/contraband-pod/fragment outcomes surfaced with the nav `CheckBreakdown`. Typed fail events (T-1003) render as notices, never silence.
**Accept:** Playwright explores through the real UI on a seeded fixture, takes the sealed-pod choice with its risk visible, gains a fragment, and the Nemesis File renders **non-empty** (the empty-state spec is extended, not left as the sole assertion); typed fails render as visible notices.

### T-1404 · Hangout & lending pane — `status: TODO` · `coder: fable` · `after: T-1403, T-1303, T-1304`
The Hangout as a visitable place in the cockpit: present-NPC list (from their simulated positions), Spacer's Dare with die commitment and both actors' opposed `CheckBreakdown`s (the honest-dice signature applied to gambling), the rumor table, and Penny Wise's desk — borrow/repay with the interest schedule visible up front, per the "dice are honest" ethos applied to money.
**Accept:** Playwright — visit, wager a die, read both actors' checks; take and repay a loan entirely through the UI; the pane is offered only where the engine says a Hangout exists; every displayed number traced to an engine export/quote.

### T-1405 · Progression, property & smuggling surfaces — `status: TODO` · `coder: opus` · `after: T-1404, T-1305, T-1306, T-1307`
Surface M13's remaining mechanics: the dawn dock renders variable hand size, the re-roll affordance, and the floor indicator (T-1306); a contraband hold indicator plus the patrol scan surfaced in the encounter overlay with its GUILE breakdown (T-1305); a port-ownership pane with quote-pattern buy preview and income ledger (T-1307). Dawn-hand spec fallout (5-dice assumptions) is this task's UI-side responsibility.
**Accept:** Playwright — with a hired crew the dock shows 6 dice and the re-roll works through the UI; a seeded patrol scan renders its GUILE breakdown and consequence; buy a port and watch income tick at dusk.

### T-1406 · Storylet delivery & diegetic shell — `status: TODO` · `coder: fable` · `after: T-1405, T-1302`
Close the PRD §8.3 drift: storylets should be delivered by the economy — "a contract, a price spike, a wire item — rather than a quest marker" — but the shipped cockpit puts them behind a badge-counted launcher button. Make storylets open from their manifest lines and wire items, replacing the launcher; fold the audio popover into settings (two popovers to reach a volume slider is the "menu ceremony" PRD §2 forbids); make the toolbar diegetic (inside the bezel / in-fiction). A sweep spec must prove no storylet becomes unreachable by the change.
**Accept:** Playwright — a cargo storylet opens from its manifest line and a wire item opens its storylet; the badge launcher is gone; a sweep spec proves every eligible offer is surfaced somewhere; `sound.spec` updated for settings-hosted sliders.

### T-1407 · Onboarding for the new verbs — `status: TODO` · `coder: opus` · `after: T-1406`
Extend the T-311 coach-prompt registry to the new verbs: explore, Hangout, loan, contraband choice, port purchase — each anchored to its real affordance, non-modal, once-each, persisted (same client-only seen-state pattern).
**Accept:** Playwright — a fresh seed fires each new prompt once, anchored correctly; dismissed state persists across reload; no prompt fires for a verb the current state cannot perform.

---

## M15 — Content (writing tasks — planner supplies voice guide from `git show f2f95fa9:foundation/lore/User-Manual.md`)

### T-1501 · Storylet batch: ports & rumors (20) — `status: TODO` · `coder: opus` · `after: T-1302, T-1303, T-1104`
(Revises old T-402.) 20 storylets keyed to systems (Algol-2's missing repair shop, rim character now that rim systems receive traffic) + Wise One / Sage audience scenes + the Hangout rumor table's authored beats (the host slot T-1303 built).
**Accept:** all load and validate; every core+rim system has ≥1 storylet reachable in a 500-day sim sweep (rim reachability is now real); the rumor table fills ≥3 dynamic slots from live NPC state; no storylet dead-ends the day.

### T-1502 · NPC personal chains (6 × 3 episodes) — `status: TODO` · `coder: fable` · `after: T-1204, T-1301`
(Revises old T-403.) Personal arcs for Silk Dagger, Doc Salvage, Wild Card, Rattlesnake, Stellar Monk, The Broker — keyed to Bond/Flaw, gating on disposition/bond state that now has teeth (T-1204), with the ignore-it-and-the-wire-resolves-it path (PRD §8.1: chains can resolve without you).
**Accept:** each chain completable and abandonable in sim; abandonment produces the wire resolution; episode gates hit organically (no test sets disposition by hand); disposition consequences asserted.

### T-1503 · Alliance arcs: faction rep + first quests — `status: TODO` · `coder: fable` · `after: T-1002, T-1305, T-1307, T-1301`
(Revises old T-404.) Four-faction reputation on `GameState` moved by organic play (patrol tribute, smuggling via T-1305, port deals via T-1307), one 3-step questline per alliance expressing its playstyle (League patrol writ, Dragons duel circuit, Confed port stake, Rebel smuggling lane), cross-faction consequences. Rep is nested state — the exact bug class T-1002 exists to stop.
**Accept:** rep nonzero after 100 trader days; an explicit nested-rep save round-trip regression test (the T-1002 bug class, by name); each questline completable in sim; joining one measurably shifts the other three dispositions; all content as data.

### T-1504 · Deed & era content pass + Conqueror headroom — `status: TODO` · `coder: opus` · `after: T-1302, T-1308`
(Revises old T-406.) Fill to launch quantity: ≥30 Deeds including new-verb deeds (gambling, smuggling, lending, exploration, property), 6 era events fully written **with storylet tie-ins via the now-real era-event trigger** (T-1302 unblocked this), rank citation texts for all 10 ranks.
**Accept:** counts met, all validate; every era reachable and fires ≥1 tied storylet in a seed sweep; no deed unearnable (200-seed sweep earns every deed at least once); a long veteran sim reaches Conqueror through play.

### T-1505 · The Nemesis Signal arc — `status: TODO` · `coder: fable` · `after: T-1310, T-1403, T-1502`
(Revises old T-405.) The career mystery: 12 fragments authored across ≥3 acquisition modes (derelict logs, Sage decodings, NPC-held pieces), the decoded-lore index text, and the endgame — the crossing chain and the v1 ending screen (Andromeda itself stays sealed for the expansion). The crossing lifts T-1101's NEMESIS destination gate via its flag and requires the PRD's stake (ship + bank commitment); Conqueror interacts per T-1308's reader.
**Accept:** full arc completable in a scripted long sim AND the acquisition funnel proven once through the real UI (Playwright: explore→fragment→Sage decode); crossing requires the stake; ending reachable and returns to menu cleanly.

---

## M16 — Hardening & balance

### T-1601 · Policy fleet v3 — `status: TODO` · `coder: opus` · `after: T-1104, T-1303, T-1304, T-1305, T-1306, T-1307`
Policies learn the new verbs: the explorer explores and decodes, the trader runs the rim and borrows under duress, the fighter uses its now-real equipment; add smuggler/gambler behaviors. The stats report gains loan usage, scan outcomes, Hangout EV, and the T-1004 `fuelStarvationDays`.
**Accept:** each policy's 300-day report renders with the new metrics nonzero where applicable; trader clears Tour One within the interim band (≥50% of 50 seeds); no shipped policy triggers a poverty-trap (criterion scoped to the competent policies, per the T-1005 errata).

### T-1602 · Tour One E2E — `status: TODO` · `coder: opus` · `after: T-1407`
(Revises old T-501.) Playwright: complete Tour One start-to-resolution through the real UI (per global test-intent rules — every step a player keystroke/click, zero engine shortcuts), both resolution branches, plus a death-and-legacy run — under a CI gate that now actually executes it (T-1001).
**Accept:** both branch tests green in CI's Playwright job; run report artifact (screens visited, days elapsed); flake rate <2% over 20 CI runs.

### T-1603 · Balance tuning from sim — `status: TODO` · `coder: fable` · `after: T-1601, T-1504`
(Revises old T-503.) Run the policy fleet across 500 seeds and tune against PRD targets — now against a game whose numbers bind: fuel scarcity (T-1102), 4× encounters (T-1103), load-bearing components (T-1205), margin scaling (T-1202). Targets: Tour One clearable by competent play in 25–30 days (not 10, not never), no dominant route (era churn working), combat EV negative below tier parity without preparation, deed pacing.
**Accept:** tuning memo in `docs/balance/` with before/after distributions; median trader debt-clear day in [22, 30]; combat EV negative below tier parity unprepared (testable now that components matter); nonzero death rate across 1,200 sim days (closing the audit's zero-deaths finding); no stable optimal route; all prior tests still green.

### T-1604 · UGT campaign & fix loop — `status: TODO` · `coder: fable` · `after: T-1003, T-1602`
(Revises old T-502.) Point UGT (sibling repo) at Rimward via the repaired adapter (T-1003). Run the autonomous playtest loop per the established memory protocol (no stopping per failure); triage findings into fixes on this list's pattern.
**Accept:** ≥1,000 UGT actions logged; every HIGH finding fixed with a regression test; `ActionBlocked` UI/protocol event parity verified during the campaign; findings report committed to `docs/playtests/`.

### T-1605 · Failure & edge hardening — `status: TODO` · `coder: opus` · `after: T-1602`
(Revises old T-504.) Error boundaries with save-preserving recovery, corrupt-save UX, the anti-poverty-trap invariant extended over the new adversarial states (indebted-to-Penny-Wise, post-confiscation, zero-fuel-rim), performance pass (1,000-day event logs).
**Accept:** invariant property test in CI covering the named adversarial states; forced crash recovers without save loss; a 1,000-day save loads <2s.

---

## M17 — Ship it

### T-1701 · Electron shell — `status: TODO` · `coder: fable` · `after: T-1602`
(Old T-601, scope unchanged.) Electron wrapper per TECH-STACK lean: local save dir (migrating localStorage saves in), window management, auto-updater stub, mac+win packaging scripts. Keep the web build working.
**Accept:** packaged app runs Tour One on macOS; saves in OS app-data; web build unaffected (CI proves both).

### T-1702 · Steamworks integration — `status: TODO` · `coder: fable` · `after: T-1701`
(Old T-602, revised.) steamworks.js (or equivalent): achievements mirrored from the ≥30-Deed set including Conqueror, Steam Cloud on the seed-carrying T-1002 envelope, rich presence (current system/day). Graceful no-Steam fallback.
**Accept:** achievements fire from deed events in the Steam dev sandbox; cloud round-trip verified; app runs identically without Steam present.

### T-1703 · Demo build (Tour One) — `status: TODO` · `coder: opus` · `after: T-1702`
(Old T-603, revised.) Demo configuration: Tour One + 3 post-resolution days, veteran features teased-but-gated — the gate list now includes Hangout progression, ports, and Conqueror content — demo-save carries into full game, distinct build flag and Steam depot config.
**Accept:** demo build produces the gate correctly (no veteran content reachable — Playwright proves it); save import works full-side; build size sane (<200MB).

### T-1704 · Release checklist — `status: TODO` · `coder: opus` · `after: T-1603, T-1604, T-1605, T-1702, T-1703`
(Old T-604, unchanged.) Final sweep: store-page asset export list, credits (fonts/audio licenses), version stamping, README/press one-pager, tag `v1.0.0-rc1`.
**Accept:** checklist doc complete with every item checked or explicitly waived by the user; RC tag builds green from clean clone.

---

## Deliberately deferred (do not scope-creep into v1)
Async arena PvP (Season 1) · Andromeda region (expansion) · Roscoe upgrade path · gambling games beyond Spacer's Dare · ports-as-property beyond the single-stake mechanic · localization · controller support.
