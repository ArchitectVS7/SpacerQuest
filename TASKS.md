# Rimward — Master Task List v1.2

Finish the build-out of *Spacer Quest: Rimward* per `docs/PRD-REIMAGINED.md` and
`docs/TECH-STACK.md`. This list supersedes `TASKS_v0.2.md` (M10–M13, T-1001–T-1310,
all DONE and verified by the truth audit of 2026-07-14: zero false Delivered claims).
It carries the 21 open tasks forward unchanged (IDs stable, satisfied prerequisites
stripped) and adds M18, a small cleanup milestone closing the audit's four
precision findings before the UI build begins. The engine layer is complete; what
remains is making it visible to a player (M14), filling content to launch quantity
(M15), hardening (M16), and shipping (M17).

**v1.2 split pass (2026-07-25):** the original T-1504 exceeded 87 minutes in a single
coding session (stopped by the user; unreviewed draft committed as `77ee7c04`) because
one task bundled content authoring, engine wiring, AND exhaustive seed-sweep proofs.
Every remaining TODO task has been split into subtasks sized for one plan→code→review→
gate→commit loop each (lettered IDs, e.g. T-1504a; commit as `T-1504a: <title>`). Two
sizing rules now apply list-wide: (1) **authoring content and proving coverage via seed
sweeps are separate tasks** — sweep-proof tasks hunt seeds with throwaway `.scratch/`
scripts and commit pinned-seed tests with sweep-provenance comments (the T-1501/T-1502
pattern), never a full re-sweep in CI; (2) a task ships **one deliverable** — if the
Accept line needs "and" between unrelated artifacts, it is two tasks.

## Orchestrator protocol

1. **Check out** the first task with `status: TODO` whose `after:` tasks are all DONE. Set it `IN-PROGRESS`.
2. **Plan** — delegate to the Fable planner with: the task block below, plus pointers to `docs/PRD-REIMAGINED.md`, `docs/TECH-STACK.md`, `TASKS_v0.2.md` (for the Delivered notes of the engine work each task surfaces), and the files named in the task. Nothing else.
3. **Code** — delegate to the coder named in `coder:` (Opus by default; Fable where flagged — those tasks have cross-cutting design decisions baked in).
4. **Review** — delegate to the Sonnet reviewer with the diff and the task's acceptance criteria. Criteria are written to be mechanically checkable AND to prove integration (reachable by a player; consumed by a named reader).
5. On pass: run the gate, commit as `T-1NNN: <title>`, set `status: DONE`, update this file in the same commit. On fail: one fix round with the coder, then escalate to Fable if still failing.

**App.tsx serialization rule** (lesson of v0.1's T-304/T-307 collision): tasks that touch `packages/ui/src/App.tsx`, `store.ts`, or `format.ts` must never be concurrently IN-PROGRESS — commit each before starting the next. The M14 chain encodes this as a strict `after:` sequence; do not parallelize it.

**Foundation reference:** the 1991 rules of record live at git ref **`f2f95fa9`** — consult them as `git show f2f95fa9:foundation/rules/<file>` (e.g. `travel.ts`, `upgrades.ts`, `combat.ts`, `constants.ts`). The writing voice guide is `git show f2f95fa9:foundation/lore/User-Manual.md`.

**Gate (every task):** `npx tsc -b` clean at root (including `packages/ui/e2e/`) + `npm run lint` (covering e2e specs) + `npm run format:check` + `npm test` all green. UI tasks additionally require `npm run test:e2e -w @spacerquest/ui` green locally. Per global rules: UX-facing verification goes through the real UI, never the engine directly.

**CI-evidence rule:** review and gate run on the uncommitted diff, so acceptance criteria must be locally checkable — the local equivalent of what CI runs satisfies review. Where a criterion names CI or push-dependent evidence, it is confirmed **after** the commit is pushed and recorded in the task's Delivered note; the CI Playwright job must be green on the branch's most recent pushed commit before the next task starts.

**Standing constraints** (reviewer enforces on every task):
1. Engine stays pure: no DOM, no I/O, no `Math.random`/`Date` — all randomness through `SeededRng`.
2. Every feature reachable headlessly; UI is a client, never the owner, of a rule.
3. All state changes emit typed `GameEvent`s; state survives JSON round-trip. Any task that adds a `GameState` field ships a save migration and a round-trip test in the same commit.
4. Content is data in `packages/content`, never logic.
5. The PRD-REIMAGINED experience wins over foundation numbers. Foundation (`f2f95fa9`) is the reference of record, consulted first; every divergence is commented at the definition site with the PRD rationale. Undocumented divergence is a review failure.
6. A task is not DONE until the feature is **reachable by a player through the UI**. Engine-only tasks must name, in their block, the task that surfaces them — and that task inherits the reachability obligation.
7. Every state field, flag, or event a task adds must **name its reader**, and the acceptance must assert the reader consumes it. If nothing reads it, it isn't a feature — it's a receipt.

**Rebalance fallout rule:** a task whose change breaks an existing green test fixes that test **in the same commit** — leaving it for later is a review failure. Balance thresholds are staged deliberately: interim loose bands (e.g. trader clears ≥50% of seeds) hold until T-1603 asserts the canonical targets; earlier tasks must not enshrine numbers T-1603 will move.

Statuses: `TODO` | `IN-PROGRESS` | `DONE` | `BLOCKED(reason)`

---

## M18 — v0.2 audit cleanups

Small, engine-side truth debt from the 2026-07-14 audit of TASKS_v0.2.md. All four
are cheap; they run first so the baselines the UI and balance work build on are
honest. None changes game behavior except where explicitly stated.

### T-1801 · Honest bond-intervention sim — `status: DONE` · `coder: opus` · `after: —`
The T-1204 acceptance claimed a bond intervention arises "from organic play", but the 300-day disposition sim in `packages/sim/src/__tests__/campaign.test.ts` (~lines 960–1081) hand-steers the player toward Doc Salvage with a scripted fly-to-Doc loop during a bond window and breaks early once both signals fire — the mechanism is real, the "organic" label is not. Either (preferred) make an unguided policy reach the bond hook — a policy that refuels opportunistically will drift into Doc's low-fuel trigger given enough days; widen the sweep horizon or seeds rather than steering — or, if that proves impractical within the task, rename/re-comment the test as a scripted reachability proof and record the overstatement as an errata entry in `docs/BALANCE-POLICY.md` Part C.
**Accept:** the test's name and comments no longer claim organic play unless the play is policy-driven with zero hand-coded steering toward Doc (reviewer checks the loop body); if re-scoped instead, the errata entry is present; the test still asserts ≥1 `BondIntervention` and peak |disposition| ≥5; full suite green.

**Delivered (2026-07-14):** Replaced the scripted fly-to-Doc loop in the T-1204 300-day sim with a genuinely unguided driver — a generic `resolveOffered` helper that answers whatever storylet is on the board by its first choice, with zero NPC-id or chain-id awareness, plus the shipped `veteranPolicy` for the day-to-day career and generic combat handling that fights named interceptors to the death once armed. Doc Salvage's distress-ping/follow-up chain is now walked only as an incidental side effect of playing every offered card, never singled out, and no line of the loop references Doc's id, his storylet chain, or travel toward his system. Re-swept seeds 1..40 at the 300-day horizon and pinned seed 33, the first to land both acceptance signals purely from unguided play: a fuel-gift bond intervention on day 7 and peak |disposition| 5 (a −5 combat grudge) on day 43. Scope boundary: the disposition mechanic itself (T-1204) is unchanged — only the test's steering was removed and the seed re-selected; no game behavior changed.

### T-1802 · Post-flip encounter-damp assertion — `status: DONE` · `coder: opus` · `after: —`
T-1301 flips `state.era` to VETERAN and `generateEncounter` (`packages/engine/src/actions/travel.ts`) only applies `TOUR_ONE_ENCOUNTER_MULTIPLIER = 0.5` while `era === 'TOUR_ONE'` — verified in source, but no test asserts the damp actually drops after the flip; the era gate could regress silently. Add a same-seed A/B (or seeded frequency comparison) proving encounter chance is undamped once the era is VETERAN, alongside the existing Tour One damping test in `encounter.test.ts`.
**Accept:** the new test goes red under a mutation that removes the era condition from the damp (mutation applied, observed red, reverted — recorded in the Delivered note); green otherwise; no fixture churn (the test forks its own rng, perturbing no golden).

**Delivered (2026-07-14):** Added a post-flip (VETERAN) undamped-encounter test in `packages/engine/src/__tests__/encounter.test.ts`, directly after the existing "Tour One damps the encounter chance 0.5x" test and as its mirror. It runs the same 1,000-seed tier-1 core sweep for both eras, each seed forking its own `new SeededRng(seed)` and calling `generateEncounter` directly, so no golden/snapshot fixture is read or written. It pins the VETERAN side to the undamped table band `[0.25, 0.35]` (~0.30) and asserts `veteran > tourOne`. Mutation verification: replaced the era-gated ternary at `travel.ts:282-285` with the unconditional `routeDangerChance * TOUR_ONE_ENCOUNTER_MULTIPLIER`; the test went red (veteran collapsed to 0.15, below the 0.25 floor and no longer > tourOne), then reverted to green. Scope boundary: test-only change — no source or game behavior touched; no new GameState field, event, or migration. The protected reader is `generateEncounter`'s `state.era` read (flipped by the day-30 resolution); the test title/comment name it and the test is the acceptance that the era gate is consumed.

### T-1803 · Action-driven fuel-capacity A/B — `status: DONE` · `coder: opus` · `after: —`
T-1102's hull-upgrade A/B (`economy.test.ts` ~149–153) calls `calculateFuelCapacity` directly rather than exercising the player-facing path. Add an end-to-end test that drives a real Shipyard hull upgrade through `applyPlayerAction` and asserts `maxFuel` rises via the `syncMaxFuel` chokepoint — proving the action→capacity wiring, not just the formula.
**Accept:** the test performs an actual `Shipyard` action on a state that can afford it and asserts `state.player.ship.maxFuel` increased afterward, without calling `calculateFuelCapacity` for the assertion; it goes red if `syncMaxFuel` is removed from `applyPlayerAction` (mutation demonstrated and reverted, recorded in the Delivered note); full suite green.

**Delivered (2026-07-15):** Added the action-path A/B test `'a real Shipyard hull upgrade raises maxFuel via the applyPlayerAction chokepoint (T-1803)'` in `packages/engine/src/__tests__/economy.test.ts`, placed immediately after the formula-only twin `'a hull upgrade raises the fuel ceiling monotonically'` (they now read as an explicit A/B pair: math vs. wiring). It sets `credits = 200000`, calls `startDay` to roll a real dawn hand and enter DAY, asserts `before === 300`, then drives a genuine `applyPlayerAction({ type: 'Shipyard', action: 'buy-component-tier', component: 'hull', tier: 2, spendDie: 0 })` and asserts `hull.strength === 20`, `maxFuel > before`, and `maxFuel === 6000` — never calling `calculateFuelCapacity`, and using `applyPlayerAction` (not `resolveShipyard`, which skips the sync). Protected reader: the `syncMaxFuel` chokepoint at `day.ts:266` — the only `maxFuel` recompute in the action path (`applyShipyardMutation` sets `hull.strength` but not `maxFuel`, and `createInitialState`/`startDay` hard-set 300). Mutation verification: commented out `syncMaxFuel(resolvedState.player.ship)` at `day.ts:266` → the new test went red (`expected 300 to be greater than 300`, `maxFuel` stuck at 300), then restored → full engine suite green (444 tests). The pre-existing twin at `shipyard.test.ts:72` was kept intentionally (a valid separate chokepoint reader), not duplicated blindly. Test-only, pure-engine change: no new GameState fields, events, migrations, or UI surfacing — the Shipyard hull upgrade is already player-reachable in the day loop; this ticket only strengthens test proof.

### T-1804 · Errata + Auto-Repair/life-support design call — `status: DONE` · `coder: opus` · `after: —`
Doc/comment-only truth pass closing the audit's prose findings. Append errata entries to `docs/BALANCE-POLICY.md` Part C: (E5) T-1003's Delivered "all 7 action types" undercounts — `PlayerAction['type']` has 11 members and the `satisfies Record<...>` guard enforces all 11; (E6) T-1101's "engine exports `isGatedDestination`/`GATED_DESTINATION_MIN_ID`" — they are `@spacerquest/content` exports consumed by the engine (`day.ts`), not engine re-exports; (E7) T-1307's era-income "A/B test" is an in-scope-vs-base lever test, not a statistical experiment. Separately, ratify the T-1205/T-1206 interaction as a design call at its definition site: `autoRepairRegen` runs before the `lifeSupportCritical` dusk gate in `day.ts` (~431–467), so the life-support succession path is unreachable whenever Auto-Repair is fitted — extend the ordering comment to state this consequence explicitly, name the covering test (`components.test.ts` ~549), and add a one-line note to T-1603's balance considerations in this file flagging it for the tuning pass (an always-rescue module may be too strong).
**Accept:** three errata entries E5–E7 present in `docs/BALANCE-POLICY.md`; the `day.ts` ordering comment names the unreachable-succession consequence and the covering test; T-1603's block in this file gains the flag; zero behavioral diff (`npm test` untouched-green).

**Delivered (2026-07-15):** Appended three errata entries (E5–E7) to `docs/BALANCE-POLICY.md` Part C correcting stale prose claims: E5 fixes T-1003's "all 7 action types" to the actual 11-member `PlayerAction` union enforced by the `satisfies Record<...>` guard at `packages/sim/src/__tests__/protocol.test.ts:234`; E6 corrects T-1101's misattribution of `isGatedDestination`/`GATED_DESTINATION_MIN_ID` — both are `@spacerquest/content` exports (`systems.ts:176,178`) consumed, not re-exported, by the engine; E7 reframes T-1307's era-income "A/B test" as an in-scope-vs-base lever comparison rather than a statistical experiment. Separately, ratified the T-1205 (Auto-Repair) / T-1206 (life-support) ordering as a ~11-line design-call comment at its definition site in `packages/engine/src/day.ts` (~435–449), stating explicitly that `autoRepairRegen` healing life support 0→1 before the `lifeSupportCritical` dusk gate makes the succession/death path unreachable whenever the module is fitted, naming the covering test (`components.test.ts` ~549) and flagging it as a T-1603 tuning-pass balance lever. The one-line flag on T-1603's block in this file was already present from a prior pass and needed no further edit. Scope boundary: doc/comment-only — zero behavioral or test changes; no code paths, constants, or test assertions were touched, matching the ticket's "zero behavioral diff" acceptance criterion.

---

## M14 — UI: honest instruments

The App.tsx chain (T-1402 → T-1403 → T-1404 → T-1405 → T-1406 → T-1407) is strictly serialized — each a single commit before the next begins. All panes are pure clients: every displayed number reads an engine export or quote.

### T-1401 · Engine export pack for UI truth — `status: DONE` · `coder: opus` · `after: —`
The audit found 11 UI-owned rules — formulas `format.ts`/`App.tsx` reimplement because the engine keeps them private. Export the truth: `componentTierForStrength` (kills the UI's invented `ceil(strength/10)` inverse that makes tier-1 unbuyable — still live at `format.ts:623`), `tributeForRound` (already engine-exported with T-1207's modifiers but ignored by the UI, which reimplements `tributeThisRound` at `format.ts:521` — the pack re-exports and the UI consumes it), `nextRankFor`, `quoteStoryletChoice` (mirroring the blessed `quoteShipyard` pattern), a typed `WireEntry.kind` at the source (kills the string-suffix flaw detection at `format.ts:326`), travel preview in real units (kills the fabricated "jumps" count at `format.ts:70`), and a fuel-affordability quote (the engine charges for fuel it clamps away — warn before commit). No behavior changes; T-1402 is the named consumer.
**Accept:** unit tests per export; `WireEntry` carries `kind`; the sim stats fixture is byte-identical (no behavior change); T-1402 named as consumer in code comments.

**Delivered (2026-07-16):** Shipped the full export pack the T-1402 UI de-rule pass will consume: `componentTierForStrength` (shipyard.ts, floor-based tier inverse fixing the tier-1-unbuyable bug), `tributeForRound` (already-exported, now doc-pointed at its UI consumer), `nextRankFor` (deeds.ts), `quoteStoryletChoice` (storylets.ts, mirroring `quoteShipyard`'s pure read-only refusal-ladder pattern), `quoteFuelPurchase` (economy.ts, previews the cost/clamp/waste of an overfilled tank), and `travelPreview` (travel.ts, real-unit distance/fuel/DC/danger replacing the fabricated "jumps" count). Added a required `WireEntry.kind` discriminator (`'flaw-override' | 'npc' | 'plain'`) stamped at every emission site across day.ts, wire.ts, deeds.ts, era.ts, legacy.ts, npc.ts, exploration.ts, port.ts, and travel.ts, replacing the UI's fragile message-suffix heuristic; bumped `CURRENT_SAVE_VERSION` to 6 with a v5→v6 migration that backfills `kind` on legacy saves by re-deriving the old suffix heuristic (the migration is the one sanctioned retro-only use of that heuristic). Unit tests added per export (deeds, economy, shipyard, storylets, wire, save-migration test files). Scope boundary, deliberately held: no UI file was touched — `format.ts`/`App.tsx` still contain all 11 reimplementations; wiring them to these exports is T-1402's job. The day-loop/storylet golden-fixture hashes moved because `kind` is now serialized on every WireEntry (a shape change), but this was verified byte-identical after stripping the new field, and the sim stats report is unaffected.

### T-1402 · UI de-rule pass — `status: DONE` · `coder: opus` · `after: T-1401`
Replace all 11 UI-owned rules in `format.ts`/`App.tsx` with the T-1401 exports. Remove the phantom TRADE-check display on contract signing (the engine spends the die and never reads its value — the instrument currently lies about the dice, the inverse of PRD differentiator 2); render signing as a die cost. Make tier-1 component purchases buyable, and fix the tier-1-hull→0-pods trap at its true owner (engine or content) in-task. Surface the fuel-overspend warning pre-commit. Fix `shipyard.spec.ts:48`, which currently bakes the tier-1 bug in as expected behavior. Wire/log spec fallout from `WireEntry.kind` is this task's to fix.
**Accept:** Playwright buys a tier-1 component (25cr) through the UI without the pods regression; the manifest sign flow renders no check; storylet locks and rank display asserted against the imported engine exports; a guard test proves `format.ts` imports rather than reimplements the listed formulas.

**Delivered (2026-07-16):** Wired `format.ts`/`App.tsx` to the T-1401 export pack, retiring all 11 UI-owned rules: `componentTierForStrength` (floor-based tier inverse — the junker now sits at tier 0, so tier 1/strength-10 is buyable), `tributeForRound` (now forwarding the interceptor's anonymous class so a Brigand/Reptiloid preview matches the engine's charge), `nextRankFor` (registry's next-rank readout), `quoteStoryletChoice` (backs `storyletChoiceLock`/`storyletChoiceCostLabel`/`storyletChoiceNeedsDie`, each now taking `game`/`storyletId` and reading the engine's own refusal ladder instead of re-deriving it from `choice.requirements`), `quoteFuelPurchase` (new pre-commit overspend/unaffordable warnings on the fuel depot), and `travelPreview` (replaces the fabricated `jumpsBetween` round with the engine's real fuel-cost figure on the manifest destination line). Also switched `wireKind` to read the engine-stamped `WireEntry.kind` instead of suffix-matching content `FLAWS` strings. Fixed the tier-1-hull→0-pods trap at its true owner, the engine's `maxCargoPodsForShip` hull-capacity boundary (`> 9` → `> 10`), which was parking a strength-10 (tier-1) hull at the foundation's zero point; added a regression test covering junker/tier-1/tier-2/tier-3 capacities. Rewrote the manifest SIGN row to render "costs 1 die" instead of the phantom "+ TRADE `<n>`" check the engine never rolls (HAGGLE remains the one real TRADE DC-12 check). Updated `shipyard.test.ts` and `shipyard.spec.ts` (now buys the real tier-1 upgrade instead of the old tier-skipping tier-2 purchase) for the tier-boundary fix, and added `derule.spec.ts`: a source-scan guard asserting `format.ts` imports (not reimplements) the six listed formulas, plus DOM assertions that the sign row shows no TRADE check, and that storylet locks/rank display match the imported engine exports on the store's deterministic default seed. Scope boundary, deliberately held: no new engine rules were added beyond the single true-owner fix (the hull-capacity boundary) called out in the ticket; every other number surfaced is a pass-through of an existing T-1401 export, and the remaining panes in the M14 chain (T-1403–T-1407) are untouched.

### T-1403 · Exploration UI — `status: DONE` · `coder: opus` · `after: T-1402`
The audit's single biggest gap: the engine's `Explore` action — nav check, 80 fuel, beacons, derelicts, salvage, contraband pods, Signal Fragments, all built and tested since T-111 — has no UI affordance whatsoever; there is no `explore()` verb in `store.ts`. Add an "off-lane sweep" control on the starmap pane; an `explore()` verb in the store (the missing action); the POI resolution flow (beacon/derelict); salvage/contraband-pod/fragment outcomes surfaced with the nav `CheckBreakdown`. Typed fail events (T-1003's `ExplorationFailed` reasons) render as notices, never silence. Audit note: the Nemesis File pane already renders non-empty entries (`App.tsx` ~1065–1090) — the gap is reaching a fragment through the UI, not rendering one.
**Accept:** Playwright explores through the real UI on a seeded fixture, takes the sealed-pod choice with its risk visible, gains a fragment, and the Nemesis File renders **non-empty** (the empty-state spec is extended, not left as the sole assertion); typed fails render as visible notices.

**Delivered (2026-07-16):** Added the missing `explore()` verb to `store.ts` — a single `applyPlayerAction({ type: 'Explore', spendDie })` call armed off a hand die, mirroring `travelTo`'s pure-client shape: the nav check reads through the shared PILOT `CheckBreakdown`, loot reads through a new `explorationOutcome` summary (composed only from the action's typed events — `PoiDiscovered`/`SalvageRecovered`/`FragmentAcquired`/`ContrabandFound`), and every `ExplorationFailed` reason (`insufficient-fuel`, `nav-check`, plus the three UI-prevented die-state reasons) maps to a visible notice via `explorationFailNoticeFrom`, never silence. Die-spend state is read off the returned hand's authoritative `spent` flag rather than inferred, since the engine burns the die before the fuel gate. Added an "OFF-LANE SWEEP" control to the Starmap pane showing the engine's own DC/fuel/effective-modifier preview (`format.ts` `explorationPreview`, reading `EXPLORATION_NAV_DC`/`EXPLORATION_FUEL_COST`/`navBonus` — no invented numbers), gated on an armed die and fuel affordability with a reason-naming label. `explorationOutcome` state is cleared on any new die selection, travel, or day-end so a stale sweep summary never lingers next to an unrelated action. Adjusted `.col.left`'s grid rows (`theme.css`) from a bare `auto` starmap row to `minmax(0, auto)` so the taller starmap pane (now carrying the persistent sweep control) shrinks and scrolls internally on short viewports instead of overflowing into the ship pane. Added `e2e/exploration.spec.ts`: a seed-45 fixture exercised entirely through the UI (die click → sweep button → CheckBreakdown → outcome readout → sealed-pod storylet choice with its risk prose visible → Nemesis File non-empty with a 1-fragment count) plus a second spec proving a failed nav check renders its typed fail as a visible notice with no loot readout. Extended (not replaced) the existing Nemesis-file empty-state spec with a pointer comment to the new non-empty companion assertion. Scope boundary, deliberately held: no new engine rules or exploration mechanics were added — this task is purely the missing UI affordance for exploration behavior that has existed in the engine since T-111; the Hangout/lending pane (T-1404) and remaining M14 chain panes are untouched.

### T-1404 · Hangout & lending pane — `status: DONE` · `coder: fable` · `after: T-1403`
The Hangout as a visitable place in the cockpit: present-NPC list (from their simulated positions), Spacer's Dare with die commitment and both actors' opposed `CheckBreakdown`s (the honest-dice signature applied to gambling), the rumor table, and Penny Wise's desk — borrow/repay with the interest schedule visible up front, per the "dice are honest" ethos applied to money. The engine side (T-1303 `VisitHangout` venues, T-1304 borrow/repay, typed fails, `quote`-style previews) is complete — this task is pure client.
**Accept:** Playwright — visit, wager a die, read both actors' checks; take and repay a loan entirely through the UI; the pane is offered only where the engine says a Hangout exists; every displayed number traced to an engine export/quote.

**Delivered (2026-07-16):** Added `visitDare`/`borrowLoan`/`repayLoan` verbs to `store.ts`, each a single `applyPlayerAction({ type: 'VisitHangout', venue: ... })` call armed off a hand die, mirroring `explore()`'s pure-client shape — the die-spend state is read off the returned hand's authoritative `spent` flag, and every typed `HangoutEvent`/`LoanEvent` fail (`no-opponent`, `already-has-loan`, `no-loan`, `insufficient-credits`, the three malformed-die reasons) maps to a visible notice via `hangoutFailNoticeFrom`/`loanFailNoticeFrom`, never silence. Both of the Dare's opposed `StatCheck` events (player's GUILE gamble vs. the dealer's counter) plus the `HangoutEvent{dare}` wager/winner/credits-delta are captured into a new `dareOutcome` client-presentation-only state field, rendered through a `CheckReadout` component split out of the existing `CheckBreakdown` so both actors' honest checks share the same inner markup as the rest of the cockpit. Added `format.ts` reads (`hangoutOpen`, `hangoutNpcs`, `hangoutRumorLines`, `dareWagerBounds`, `lendingTerms`) — `hangoutOpen` gates on the exact `STAR_SYSTEMS[id].hasHangout` flag `day.ts` checks, `hangoutNpcs` filters on live NPC simulated position, `hangoutRumorLines` is a pure pass-through to the engine's own exported `hangoutRumors` (never re-synthesized), and the lending terms are shown from raw content constants up front — the realized dusk interest is never recomputed client-side. Built the `HangoutPanel` in `App.tsx`: an "AT THE TABLES" opponent picker, the Spacer's Dare wager control with both actors' `CheckReadout`s and a won/lost verdict line, the rumor table, and Penny Wise's desk (borrow form when no loan is outstanding, status + repay form when one is). The launcher button is offered only when `hangoutAvailable` (Hangout exists in-system AND no live encounter/aftermath/day-30 ceremony). Added `theme.css` styling for the panel and `e2e/hangout.spec.ts` covering all three acceptance flows: wager-and-read-both-checks, take-and-repay-a-loan, and Hangout-offered-only-where-the-engine-says-one-exists. Scope boundary, deliberately held: no engine changes — T-1303/T-1304's `VisitHangout` venues, typed fails, and lending state were already complete; this task is purely the missing UI affordance. The dawn dock, contraband/patrol surfaces, and port-ownership pane (T-1405) and the remaining M14 chain are untouched.

### T-1405 · Progression, property & smuggling surfaces — `status: DONE` · `coder: opus` · `after: T-1404`
Surface M13's remaining mechanics: the dawn dock renders variable hand size, the re-roll affordance, and the floor indicator (T-1306); a contraband hold indicator plus the patrol scan surfaced in the encounter overlay with its GUILE breakdown (T-1305); a port-ownership pane with quote-pattern buy preview (`quotePort` exists) and income ledger (T-1307). Dawn-hand spec fallout (5-dice assumptions, including the onboarding "Five dice, once a day" copy) is this task's UI-side responsibility.
**Accept:** Playwright — with a hired crew the dock shows 6 dice and the re-roll works through the UI; a seeded patrol scan renders its GUILE breakdown and consequence; buy a port and watch income tick at dusk.

**Delivered (2026-07-16):** Added `hireCrew`/`dismissCrew`/`reroll`/`buyPort` verbs to `store.ts`, each a pure client of the existing T-1306/T-1307 `Crew`/`Reroll`/`Port` engine actions — hire/dismiss/buy arm off a hand die (die-spend read off the returned hand's authoritative `spent` flag), while re-roll deliberately does NOT consume a selected die, spending a `rerollsRemaining` charge against a targeted `dieIndex` instead. Every typed fail (`CrewEvent{failed}`, `PortEvent{failed}`, `DiceRerolled{failReason}`, including the malformed-die reasons) maps to a visible notice via new `crewFailNoticeFrom`/`portFailNoticeFrom`/`rerollFailNoticeFrom` translators, never silence. A jump-triggered PATROL contraband scan (engine `actions/patrol.ts`) is captured into a new client-only `patrolScan` state field — the interceptor's own `ContrabandScan`/`ContrabandConfiscated` events (GUILE check, caught flag, fine, seized-cargo flags) — cleared alongside the rest of the encounter-adjacent state on selection/new-day/new-game/aftermath-dismiss/slot-load. Added `format.ts` pure reads (`dawnHandModifiers`, `crewRoster`, `crewBenefitLabel`, `portLedger`, `portFailureExplanation`, `contrabandHold`), each sourced from an engine export (`dawnDiceModifiers`, `quotePort`, `crewCapacity`, `isCarryingContraband`/`isCarryingIllicit`) or raw content constants — no rule is re-derived client-side. Built in `App.tsx`: the `PatrolScanReadout` (GUILE `CheckReadout` + caught/fine/seizure line) rendered inside the combat overlay; a `CrewSection` in the ship pane listing hired crew (with dismiss) and hireable roles (with disabled-reason gating on berth-then-affordability, mirroring `resolveCrew`'s own check order); a `PORT AUTHORITY` ledger block in the trade pane showing the current system's buy preview plus every owned stake's per-dusk income and a running total; and a `CONTRABAND HOLD` badge that lights whenever the ship carries an illicit contract or sealed pod — exactly the condition a patrol scan gates on. Reworked the dawn dock's die rendering to size off the live hand (not a hardcoded five) and added a per-die re-roll control gated on remaining charges, plus a floor badge. Updated the onboarding "dawn-roll" prompt copy to be hand-size-neutral (a hired First Officer grows the hand past five). Added `theme.css` styling for all of the above and `e2e/progression.spec.ts` covering the three acceptance flows verbatim: hired-crew dock shows 6 dice and re-roll works through the UI, a seeded patrol scan renders its GUILE breakdown and consequence, and buying a port ticks income at dusk. Scope boundary, deliberately held: no engine or content changes — T-1305 (patrol/contraband), T-1306 (crew/dice progression), and T-1307 (ports) were already complete; this task is purely the missing UI affordances for mechanics the engine already runs. The storylet-delivery shell (T-1406) and onboarding for these new verbs beyond the one dawn-roll copy fix (T-1407) are untouched.

### T-1406 · Storylet delivery & diegetic shell — `status: DONE` · `coder: fable` · `after: T-1405`
Close the PRD §8.3 drift: storylets should be delivered by the economy — "a contract, a price spike, a wire item — rather than a quest marker" — but the shipped cockpit puts them behind a badge-counted launcher button (`App.tsx` ~384–402). Make storylets open from their manifest lines and wire items, replacing the launcher; fold the audio popover into settings (two popovers to reach a volume slider is the "menu ceremony" PRD §2 forbids); make the toolbar diegetic (inside the bezel / in-fiction). A sweep spec must prove no storylet becomes unreachable by the change.
**Accept:** Playwright — a cargo storylet opens from its manifest line and a wire item opens its storylet; the badge launcher is gone; a sweep spec proves every eligible offer is surfaced somewhere; `sound.spec` updated for settings-hosted sliders.

**Delivered (2026-07-16):** Replaced the badge-counted storylet launcher with a pure, TOTAL `storyletSurface` classifier (new in `format.ts`) that routes every authored storylet id by prefix to one of four diegetic surfaces — `hold` (cargo/derelict/fence), `wire` (wire/guild bulletins), `port` (everything else, including the reachability-guaranteeing default), and `ceremony` (the day-30 resolution, still owned solely by the existing full-screen `ResolutionCeremony`). `availableStorylets`/`offersForSurface` project the engine's live offer set through that classifier for each cockpit surface to consume. `StoryletPanel` was reworked from a pager over the whole offer list into a single-id-focused panel (`StoryletOpener` buttons render per-offer, each opening `App`'s new `openStoryletId` state directly — no launcher, no prev/next pager), with a `useEffect` that clears the open id the moment its offer drains from the live set so a stale id can never re-mount the panel. Wired the three diegetic openers into their real anchors: `TradePane` renders `PORT DISPATCHES` in the port-ledger block and `HOLD · something wants attention` inside the active-contract/hold block (proving both "opens from its manifest line" and the port default), and `Wire`'s cap bar grew a `BULLETIN` row for wire-surface offers. Folded the `AudioPanel` popover into `SettingsPanel` as an inline `AudioMixer` section — audio is now one popover away, not two — and made the top control cluster diegetic by moving it inside `Bezel` (a `children`-rendered `.ctrls` row on the bezel frame, in-fiction, rather than a floating top-right toolbar); same buttons/testids, `audio-toggle` and the storylet-toggle button are simply gone. Added a visually-hidden `storylet-offer-audit` list in `App.tsx` reflecting the engine's own live non-resolution offer ids, read by the new `e2e/storylet-delivery.spec.ts` sweep test, which asserts every shipped `STORYLETS` entry classifies to a real surface (classifier totality, proved at the source) and that every currently-live offer has exactly one diegetic opener that opens it on click — the "no storylet becomes unreachable" guarantee — plus dedicated flows for a cargo storylet opening from its manifest line and a Guild wire bulletin opening its letterheaded panel. Updated `derule.spec.ts`, `exploration.spec.ts`, and `storylet-registry.spec.ts`'s `showStorylet`/`openSealedPod` helpers to click the per-offer opener instead of paging a launcher, and `sound.spec.ts` to open Settings (not a separate Audio popover) before asserting on the mixer testids. `theme.css` gained styles for the new `.storylet-open` buttons, `.hold-dispatches`/`.port-dispatches` blocks, `.wire-bulletins`, the bezel's `.bezel-right`/`.ctrls` layout, and the relocated `.audio-mixer`; removed the now-dead pager (`.sl-pager`/`.sl-page-btn`/`.sl-count`) and standalone `.audio-panel`/`.ctrls button.storylet-launch` rules. `e2e/tsconfig.json` added a project reference to the UI package itself (`format.ts` is now imported directly from a spec). Scope boundary, deliberately held: no new storylet content, no change to which storylets are eligible or their requirements/costs/checks — this task only changes how an already-eligible offer is *presented and opened*; the resolution-ceremony path, `resolveStorylet`'s mutation, and every engine-side storylet rule are untouched. Onboarding for the new verbs (T-1407) is untouched.

### T-1407 · Onboarding for the new verbs — `status: DONE` · `coder: opus` · `after: T-1406`
Extend the T-311 coach-prompt registry (currently 4 prompts: combat/hand/manifest/starmap in `format.ts` ~855–877) to the new verbs: explore, Hangout, loan, contraband choice, port purchase — each anchored to its real affordance, non-modal, once-each, persisted (same client-only seen-state pattern).
**Accept:** Playwright — a fresh seed fires each new prompt once, anchored correctly; dismissed state persists across reload; no prompt fires for a verb the current state cannot perform.

**Delivered (2026-07-16):** Extended the `ONBOARDING_PROMPTS` registry (`format.ts`) with five new-verb coach prompts — `first-hangout`, `first-loan`, `first-contraband`, `first-port`, `first-explore` — each a pure predicate over existing engine/content state (the exact gate its taught affordance is live on: `hangoutOpen`, an outstanding loan check, a contraband offer on the manifest board, an unowned purchasable port at the current system, and the exploration fuel-affordance check). Added two new `OnboardingAnchor` values (`hangout`, `port`) and a new `OnboardingMount` type/`onboardingMount()` router so a prompt can render at screen level, inside the combat overlay, or — new for `first-loan` — inside the open Hangout panel itself, since that nudge's affordance only exists while the panel is mounted. Added an `autoDismiss?: boolean` flag on `OnboardingPrompt`: all five new prompts set it `false` so `nextOnboardingSeen`'s auto-dismiss reconcile skips them — they gate on affordance/location state rather than on a taught action landing, so leaving a Hangout or rerolling the board can't spuriously mark them seen before the player ever acts; only the explicit "Got it" dismissal consumes them, matching the accept criterion's "no prompt fires for a verb the current state cannot perform" by construction (the predicate IS the affordance check). Wired the `hangout`/`port` anchors into `App.tsx`'s existing `OnboardingCallout` (now parameterized on the 3-way `OnboardingMount` rather than a boolean, with the screen mount suppressed while the combat overlay is up so a lower-priority prompt can't render behind it) and mounted the `loan` instance inside `HangoutPanel`. Added `theme.css` placement rules for the two new screen anchors (stacked in the bezel's top-right rail) and the in-panel `loan` mount (static-positioned at the panel top, mirroring the combat coach's in-overlay treatment), plus responsive centering at the existing 720px breakpoint. Extended `e2e/onboarding.spec.ts`: the existing fresh-seed delivery test now continues past the guided chain into the two contextual prompts the delivery's landing system actually triggers (`first-port` then `first-explore`, at Pollux-7), and five new dedicated tests — one per verb — each proving fire-once-anchored-correctly and reload persistence through the real DOM (no API shortcuts), via a new `bootOnboardingFixture` helper that seeds `sq.save.v1`/`sq.onboarding.v1` via `page.addInitScript` without clobbering an existing save. Scope boundary, deliberately held: no new engine rules or predicates were invented — every gate is a read of state T-1401–T-1406 already exposed (`hangoutOpen`, `explorationPreview`, `isPurchasablePort`, `CARGO_TYPES[...].isContraband`, `game.player.loan`); this closes the M14 App.tsx chain (T-1402→T-1407) with no further onboarding work outstanding.

---

## M15 — Content (writing tasks — planner supplies voice guide from `git show f2f95fa9:foundation/lore/User-Manual.md`)

### T-1501 · Storylet batch: ports & rumors (20) — `status: DONE` · `coder: opus` · `after: —`
20 storylets keyed to systems (Algol-2's missing repair shop, rim character now that rim systems receive traffic) + Wise One / Sage audience scenes + the Hangout rumor table's authored beats (the host slot T-1303 built). Audit note: ~3 system-keyed port/rumor storylets exist today (`port.sun3.guild-auditor`, `wire.rimward.polaris-signal`, `wire-the-guild`) out of the 152 total — this batch is net-new writing, not relabeling.
**Accept:** all load and validate; every core+rim system has ≥1 storylet reachable in a 500-day sim sweep (rim reachability is now real); the rumor table fills ≥3 dynamic slots from live NPC state; no storylet dead-ends the day.

**Delivered (2026-07-16):** Appended 20 net-new storylets to `packages/content/src/storylets.ts` (content order preserved, originals still the leading prefix): nine mandatory systemIds-only port beats — one at each system that lacked one (Aldebaran-1, Fomalhaut-2, Vega-6 core; Antares-5, Capella-4, Polaris-1, Mizar-9, Achernar-5, Algol-2 rim, incl. Algol-2's "no repair facilities — the frontier"); six richer rim-character beats (the sealed Andromeda Operations Room, Capellan herbal run, gem cutters, frontier justice, liquor-hall gossip, ice harvest); four Wise One / Sage audience scenes (counsel, parable, constellation quiz, star-lore — pure counsel, distinct from the T-113a/T-1310 fragment brokers); and one extra core Fomalhaut vignette. Every storylet carries ≥1 requirement-free choice and uses no held (.aboard/.riding) flags, so the existing "never dead-ends the day" / "held-flag clearer" engine tests cover AC4. Moved the rumor-table's two inline engine templates into `packages/content/src/hangout.ts` as authored data (`RUMOR_TEMPLATES`, keyed by NpcActionType with warm/cold variants + quiet/empty lines); rewrote engine `hangoutRumors` to select by live `lastAction.type` and `disposition` sign and interpolate live NPC fields — the engine now owns only selection+interpolation, the prose is content. UI surfacing needed NO change: `format.ts storyletSurface` is total, so the new `port.*`/`wise-one.*`/`sage.*` ids land on the Port-Ledger opener and `hangoutRumorLines` passes the rumor lines through (the existing `storylet-delivery.spec.ts` sweep continues to prove one opener per offer). Tests: extended `engine/storylets.test.ts` (T1501 ids present + length, AC1) and `engine/hangout.test.ts` (≥3 dynamic slots from distinct live NPCs, disposition-live, AC3); new `sim/system-storylet-coverage.test.ts` (AC2 — an honest drive-upgrading trading hunter covers all 20 core+rim systems in a seed sweep, ~25s). No new GameState field or GameEvent, so NO save migration. Regenerated four deterministic golden fixtures (day-loop-golden, replay-golden) whose scripts dock at Aldebaran-1 where the new grain-exchange beat now offers — content-addition churn, rngStates unchanged. Re-pinned two seed-pinned emergent campaign tests whose unguided trajectories shifted because `veteranPolicy` plays every offered storylet (port-buy seed 3→6, bond-intervention seed 33→8); both features verified still broadly reachable through play (port acceptance hits 16/40 seeds). Full gate green: `tsc -b`, `eslint`, `prettier --check`, engine 472 + sim 59 tests.

### T-1502 · NPC personal chains (6 × 3 episodes) — `status: DONE` · `coder: fable` · `after: —`
Personal arcs for Silk Dagger, Doc Salvage, Wild Card, Rattlesnake, Stellar Monk, The Broker — keyed to Bond/Flaw, gating on disposition/bond state that now has teeth (T-1204), with the ignore-it-and-the-wire-resolves-it path (PRD §8.1: chains can resolve without you). Audit note: only Doc Salvage has a chain today (`chain.doc-salvage.distress-ping`/`.follow-up`) — extend it to 3 episodes and author the other five from zero (Silk Dagger, Wild Card, Rattlesnake, Stellar Monk have no storylet mentions at all).
**Accept:** each chain completable and abandonable in sim; abandonment produces the wire resolution; episode gates hit organically (no test sets disposition by hand); disposition consequences asserted.

**Delivered (2026-07-16):** Authored all 18 episodes (6 NPCs × 3, including the third Doc Salvage episode extending the pre-existing two) in `content/storylets.ts`, each carrying a `wireResolution` (grace-day deadline, Galactic-Wire message, disposition-penalty effects) so an ignored chain resolves itself per PRD §8.1. Added validation for the new field (`storyletValidation.ts`: non-negative `graceDays`, non-empty `wireMessage`, effects validated through the existing choice-effects path, and a check that only scheduled storylets may carry a `wireResolution`) and a new pure engine sweep `resolveAbandonedChains` (`engine/storylets.ts`) wired into `endDay`'s dusk phase (`engine/day.ts`), which resolves any scheduled episode past `dueDay + graceDays` through the same `applyEffects` path a played choice uses — identical `DispositionChanged`/`StoryletEffectApplied` events, plus a `WireEntry` for the ticker. Coverage: a sim-level test per chain for organic completion (episode gates hit through legal play, no hand-set disposition) and per-chain abandonment (wire resolution fires, disposition penalty lands) in `sim/__tests__/npc-chains.test.ts`, plus one real-UI Playwright spec (`ui/e2e/npc-chains.spec.ts`) proving the Doc Salvage 3-episode chain end-to-end through the actual storylet panel, per the test-intent rule against API-shortcut UX tests. New systemIds-gated episode-1 openers shifted unguided-sim seed timing, so two existing campaign-reach tests were re-pinned to newly-qualifying seeds with updated sweep-provenance comments (feature behavior unchanged, only which seed lands the acceptance signal first). Scope boundary: the other five alliance/era/nemesis content tasks (T-1503–T-1505) and their cross-chain rep effects are explicitly out — this task is the six personal chains and their abandonment path only.

### T-1503 · Alliance arcs: faction rep + first quests — `status: DONE` · `coder: fable` · `after: —`
Four-faction reputation on `GameState` moved by organic play (patrol tribute, smuggling via T-1305, port deals via T-1307), one 3-step questline per alliance expressing its playstyle (League patrol writ, Dragons duel circuit, Confed port stake, Rebel smuggling lane), cross-faction consequences. Rep is nested state — the exact bug class T-1002 exists to stop. This task redeems the deferrals recorded by name in code: `patrol.ts:106` and `contraband.ts:37` (the `fence.ray.dealt` flag's Rebel-rep consequence), and the confederation-tagged ports in `content/ports.ts` (the Warlord Confederation reader).
**Accept:** rep nonzero after 100 trader days; an explicit nested-rep save round-trip regression test (the T-1002 bug class, by name); each questline completable in sim; joining one measurably shifts the other three dispositions; all content as data; the named code deferrals (`patrol.ts`, `contraband.ts`, `ports.ts`) are consumed and their comments updated.

**Delivered (2026-07-16):** Added `PlayerState.reputation`, a nested `{ league, dragons, confederation, rebels }` container (`content/factions.ts` for the `FactionId`/`FACTION_IDS`/tuning constants, `engine/reputation.ts`'s single `applyReputation` clamp-and-emit mover mirroring `applyDisposition`'s shape) moved by three organic sources: patrol tribute paid/evaded (`actions/combat.ts` resolveEncounter), a caught contraband scan (`actions/patrol.ts`, now consuming both the `patrol.ts:106` deferral and the `contraband.ts:37` fence-flag Rebel-rep consequence), and buying an aligned port stake (`actions/port.ts` resolvePortPurchase, reading the `alliance` tag now type-aliased directly to `FactionId` so it can't drift from `ports.ts`'s Warlord Confederation reader). Authored one 3-step questline per faction in `content/storylets.ts` (League patrol writ, Dragons duel circuit, Confederation port stake, Rebel smuggling lane), each with a wire-abandonment path (episode 2) and a terminal join choice that grants a large own-faction bonus while cooling the other three (`FACTION_JOIN_OWN_BONUS`/`FACTION_JOIN_CROSS_PENALTY`). Persistence: `CURRENT_SAVE_VERSION` bumped to 7 with a v6→v7 migration backfilling the neutral container faction-key by faction-key, `.strict()`-schema'd per the T-1002 drift-protection law, and covered by an explicit nested round-trip regression test asserting deep-equal survival through create→migrate→load plus a strict-schema unknown-key rejection test (`engine/__tests__/save.test.ts`). Test coverage: unit tests for the mover and all three organic sources including the source-scan guard proving the named deferral comments are gone (`engine/__tests__/reputation.test.ts`), a 100-day organic-play sim proving nonzero standing without hand-injected rep plus one sim test per questline for organic completion and abandonment (`sim/__tests__/alliance-arcs.test.ts`), and one real-UI Playwright spec driving the League patrol writ through the actual storylet panel and asserting the cross-faction shift on join (`ui/e2e/alliance-arcs.spec.ts`), per the test-intent rule against API-shortcut UX tests. Scope boundary: T-1504's era-event storylet tie-ins and T-1505's Nemesis arc are untouched by this task; the reputation numbers themselves are marked INTERIM, owned by T-1601/T-1603's balance pass, not canonical tuning.

### T-1504a · Deed batch to launch quantity — `status: DONE` · `coder: opus` · `after: —`
Author the ≥13 net-new Deeds (17 exist; target ≥30 total) including new-verb deeds (gambling, smuggling, lending, exploration, property), plus their validation and engine trigger wiring. **WIP triage:** the unreviewed draft at `77ee7c04` already contains candidate work (`content/deeds.ts` +438, `deedValidation.ts`, `engine/deeds.ts` extensions, `deeds.test.ts` additions) — start by triaging that diff: keep what survives review, rewrite what doesn't; it never passed the gate, so it is a draft, not a baseline. Scope is authoring + validation + per-deed unit tests only — **no seed sweeps** (T-1504d owns all coverage proofs).
**Accept:** ≥30 deeds load and validate; every net-new deed has a unit test proving its trigger fires from a constructed qualifying state; ≥1 deed each for gambling, smuggling, lending, exploration, and property; full gate green.

**Delivered (2026-07-26):** Triaged the `77ee7c04` WIP into a 44-deed slate (27 net-new over the old 17, exceeding the ≥13 floor), covering every required new verb — gambling (the Hangout's Spacer's Dare), smuggling (`run_seized` added as the loss-side counterpart to the existing win, and the consumer that makes the `ContrabandConfiscated` allowlist entry load-bearing), lending, exploration, and property — each with a `deedValidation.ts`-checked unit test proving its trigger fires from a constructed qualifying state. Added a new `StoryletDeedProgress`-shape validation rule (a storylet-fed deed without `count` is silently unearnable; `match` on one is meaningless since the engine never event-matches it) after finding the failure mode by inspection. Extracted the three engine call sites' duplicated `JSON.parse(JSON.stringify(state))` cloning helper into a single `engine/clone.ts` (`cloneState`, with its own unit test) used by `day.ts`, `storylets.ts`, `actions/combat.ts`, and `actions/shipyard.ts` — a mechanical dedup, not new behavior. Documented, but deliberately did not fix, a balance consequence surfaced by the larger slate: rank now climbs on absolute deed count against `RENOWN_DEED_THRESHOLDS` still calibrated for 17 deeds, so GIGA_HERO lands around day 100 instead of day 200, pinning `player.tier` at 5 earlier than the old economy assumed. Re-pinned the three long-horizon sim tests this shifted (`campaign-reach.test.ts` seed 3→2, `campaign.test.ts`, `alliance-arcs.test.ts`) and regenerated the `replay-golden.ts` protocol goldens (two extra `DeedEarned` entries; `rngState` verified unchanged); `campaign-reach.test.ts`'s old seed-3 pin was confirmed already red at pre-T-1504 commit `a5dabd76`, so that re-pin repairs a pre-existing failure rather than covering a new one — no assertion was weakened at any site. Scope boundary: rebalancing `RENOWN_DEED_THRESHOLDS` to the larger slate is explicitly deferred to T-1603 (balance), not this task; the 200-seed coverage sweep and Conqueror-through-play reachability proof are T-1504d's, not attempted here; rank citation texts belong to T-1504c and are untouched beyond a comment cross-reference update.
Orchestration: graphify=none — no `graphify-out/graph.json` in the repo root. · attempts=2/4.

### T-1504b · Era-event storylet tie-ins — `status: DONE` · `coder: opus` · `after: T-1504a`
Storylet tie-ins for the 6 already-written era events, via the now-real era-event trigger (T-1302). Sequenced after T-1504a only because both triage the same `77ee7c04` WIP diff (`content/storylets.ts` +375, `era-storylet-coverage.test.ts` scaffolding) — same triage rule: draft, not baseline. Unit/integration-level proof only; sweep-level reachability is T-1504d's.
**Accept:** each of the 6 era events has ≥1 tied storylet that validates and is offered in a test that fires the event directly; no tie-in dead-ends the day; every tie-in reaches a surface via the total `storyletSurface` classifier (sweep spec still green); full gate green.

**Delivered (2026-07-26):** Re-triaged the interrupted `77ee7c04` era-tie-in draft against Standing Constraint #7 ("if nothing reads it, it isn't a feature, it's a receipt") and cut it from 20 write-only `era.*` choice-memory flags to exactly 3, each carrying a comment naming its reader at the definition site: `era.blockade.papers` (read by `era.blockade.cordon-run`'s trigger), `era.dilithium.berth` (read by `era.dilithium.claim-jumper`'s trigger), and `fence.ray.dealt` (read by the T-1305 contraband-scan concealment check in `actions/patrol.ts`) — every other choice keeps its credits/fuel/`reputation`/`disposition`/`deedProgress` payload instead, since that memory is already consumed elsewhere (`applyReputation`'s alliance questlines, `applyDisposition`'s T-1502 chains, `evaluateDeeds`'s ledger). Routed all `era.*` tie-in storylets onto the wire surface in `format.ts`'s `storyletSurface` classifier, matching PRD §8.3's "delivered by the economy ... a wire item" and needing no `App.tsx` change since the Wire opener already renders offers generically; added a Playwright assertion (`storylet-delivery.spec.ts`) that every `era.*` id resolves to `'wire'` and that every era-triggered storylet regardless of id prefix reaches a real opener (never `'ceremony'`), which is also what keeps the non-`era.*` tie-in `cargo.medicinals.plague-relief` correctly on `'hold'` instead of being forced onto the wire. Re-ran (without extending) the inherited `era-storylet-coverage.test.ts` per-defId reachability sweep to confirm the flag-gate rework of the two optional tie-ins didn't break it, and left a provenance comment marking its seed budget/horizon/wider-bar finishing as T-1504d's. Extended `storylets.test.ts` for the new flag shapes and trigger gating. Scope boundary: the 200-seed coverage sweep and Conqueror-through-play reachability proof stay T-1504d's; the credit/fuel numbers in this batch are explicitly INTERIM, owned by T-1603's balance pass, not tuned here.
Orchestration: graphify=none — no `graphify-out/graph.json` in the repo root. · attempts=1/4.

### T-1504c · Rank citation texts — `status: DONE` · `coder: opus` · `after: —`
Rank citation texts for all 10 ranks, in the `f2f95fa9` User-Manual voice. Pure writing task; the reader is the existing rank display (`nextRankFor` readout, T-1401/T-1402).
**Accept:** all 10 ranks carry citation text as content data; texts load and validate; the rank display renders the citation for the current rank (asserted in an existing or extended spec); full gate green.

**Delivered (2026-07-26):** **Truth-debt correction first:** T-1504a's Delivered note above claims "rank citation texts belong to T-1504c and are untouched" — that is **wrong**. Its triage of the `77ee7c04` WIP landed all ten `citation` strings in `content/deeds.ts`, made the field REQUIRED on `RenownRankDefinition`, and deleted the engine's generic fallback branch; one of those strings is even embedded in the `replay-golden.ts` protocol golden T-1504a regenerated. So this task was not the writing task its title describes — the writing was already done, and what was actually missing was the other two thirds of its own acceptance. Delivered here: (1) **Validation** — new `validateRenownRanks`/`defineRenownRanks` in `content/deedValidation.ts`, mirroring `defineDeeds`'s error-collection and throw-on-import contract (content stays DATA; the wrapper is a load-time shape guard on the `defineDeeds`/`defineStorylets` precedent), with `RENOWN_RANKS` now wrapped so a malformed ladder fails the build rather than a test. Five rules, each commented with the reader it protects and each a real failure mode: non-empty `label`; `def.id` must equal its key (`RENOWN_RANK_ORDER = Object.keys(RENOWN_RANKS)` drives `renownRankIndex` → `rankTier` → encounter matchmaking while `id` is what gets stored, so a mismatch desyncs the ladder silently); non-empty `citation`; **no `{`/`}` in a citation** — the structural OPPOSITE of `citationTemplate`'s mandatory `{day}`, because both rank-citation readers print it verbatim and a placeholder would ship literal braces to the player; and pairwise-distinct citations (the rank-up wire entry IS the citation, so duplicates make two promotions indistinguishable). Threshold monotonicity is deliberately NOT validated — `RENOWN_DEED_THRESHOLDS` is T-1603's and is flagged for rescale. (2) **The missing reader** — before this, a citation's only consumer was the engine's one-frame rank-up `WireEntry`, so a player who missed the ticker never saw their own citation. Added `DeedRegistryView.rankCitation` (a straight `RENOWN_RANKS[registry.renownRank].citation` lookup in `ui/format.ts`, no UI-owned rule) rendered as `registry-rank-citation` in `RecordsOverlay`'s `.registry-rank` block, with a `.rr-citation` rule matching `.rd-citation`. The bezel `rank` chip is untouched — prose does not belong in a chip. (3) **Assertions** — `derule.spec.ts`'s T-1402 rank-readout spec extended to check the citation at TWO ranks in one run through the real cockpit (day-1 LIEUTENANT, then the post-deed rank from `nextRankFor`), both against imported `RENOWN_RANKS` entries and never literals: one check alone could pass against a constant string, the pair proves the display renders the CURRENT rank's citation. Plus five `defineRenownRanks` rejection tests and an accept-the-shipped-table test in `engine/__tests__/deeds.test.ts` (validator tests live there because `packages/content` has no runner), and the existing distinct-citation test extended with the same no-placeholder loop the validator enforces. **Voice pass: all ten lines kept byte-identical as authored.** The ladder's escalating Registry verbs (opens/confirms/raises/enters/seats/stamps/elevates/files/writes/seals) already read as one voice; the divergence comment was verified correct against `f2f95fa9`'s User-Manual §Appendix A (a bare nine-row point-threshold table with no prose at all) and extended rather than duplicated, and the `T-1504` header re-tagged `T-1504a (authored) / T-1504c (validated + surfaced)`. One line was polished and then **reverted**: COMMANDER's "one deed on the board" states `RENOWN_DEED_THRESHOLDS.COMMANDER` (1) in prose and goes stale on T-1603's rescale, but that exact string is embedded in the `replay-golden.ts` golden, and rewording it turned the protocol golden red (confirmed by stash-and-rerun that the baseline was green, so the failure was mine alone). Rather than move a golden for a cosmetic edit, the coupling is flagged in a comment at the definition site naming T-1603 as the owner and the replacement wording. No other rank citation names a number. **No `GameState` field, event, or flag was added, so there is no save migration and no round-trip test — do not look for one.** Engine source untouched (purity trivially preserved); `replay-golden.ts` and every long-horizon sim seed are byte-unmoved. Gate: `npx tsc -b`, `npm run lint`, `npm run format:check`, `npm test` (572 + 82), and `npm run test:e2e -w @spacerquest/ui` (67) all green. Scope boundary: the `RENOWN_DEED_THRESHOLDS` rescale is T-1603's, seed sweeps and Conqueror-through-play are T-1504d's, and the day-30 `ResolutionCeremonyView` (which could also show a citation) was left alone.
Orchestration: graphify=none — no `graphify-out/graph.json` in the repo root. · attempts=1/4.

### T-1504d · Deed/era/Conqueror coverage sweeps — `status: DONE` · `coder: opus` · `after: T-1504a, T-1504b, T-1504c`
The proof pass split out of old T-1504 — the exhaustive sweeps are what made the single-session version blow up. Reuse/finish the `deed-hunter.ts` harness from `77ee7c04` (`packages/sim/src/__tests__/support/`). Hunt qualifying seeds with throwaway `.scratch/` scripts (not committed); committed tests pin found seeds with sweep-provenance comments — CI never re-runs the 200-seed hunt. Conqueror (threshold 30) becomes reachable exactly when the deed count lands — its reachability sweep is this task's obligation, per T-1308's deferral.
**Accept:** a recorded 200-seed sweep earns every deed at least once (sweep evidence in the Delivered note; committed test asserts pinned representative seeds); every era reachable and fires ≥1 tied storylet in the sweep (same evidence pattern); a long veteran sim reaches Conqueror through play; committed suite runtime grows <60s; full gate green.

**Delivered (2026-07-26):** A **proof-and-pin** pass, not an authoring pass — the headline finding is that **no content change was needed**: the inherited `deed-hunter.ts` harness already earns the full 44-deed slate, so nothing in `packages/content/src/deeds.ts` was retuned (comments only). **Environment prerequisite, recorded because every number below depends on it:** `packages/sim` has no vitest alias config and resolves `@spacerquest/engine`/`@spacerquest/content` through `package.json#main` to `dist/`, so every sweep and every sim run here was preceded by `npx tsc -b`; hunting against a stale `dist/` would pin seeds CI cannot reproduce.

**DEED SWEEP EVIDENCE — seeds 1..200, horizon 300 days**, driver `driveCompetentCampaign(deedHunterPolicy, seed, 300)`, run out of tree in throwaway `.scratch/` scripts (8 parallel chunks of 25; `.scratch/` is gitignored and nothing from it is committed). **Union = 44/44 authored deeds; none never-earned.** 81 of 200 seeds are individually total (44/44). 164 of 200 reach CONQUEROR; median crossing **day 63**, earliest 48, latest 244. Per-deed hit counts, rarest first: `slipped_the_scan` **92/200** (the clear long pole — it needs a patrol interdiction while illicit cargo is still aboard, which is why the hunter's "stay dirty" branch declines Smuggler Ray while that deed is outstanding); then `mercy_runner`/`known_to_the_league`/`run_seized` 153, `landlord` 156, `rentier` 157, `cartographer`/`port_authority`/`cold_case` 160, `paid_in_full`/`rich_hulk` 161, `cargo_expansion`/`contraband_run` 164, `ray_s_ledger` 166, `beacon_chaser` 167, `derelict_boarder` 168, `first_combat_win`/`first_chart`/`signal_hunter` 170, `tour_one_cleared`/`table_regular` 171, `broker_shark`/`dare_won`/`high_roller` 172, `dare_first`/`fat_manifest` 173, `signed_the_crew` 174, `debt_cleared` 178, `first_marker`/`bad_paper`/`deep_water` 179, `yard_rat` 182, `rimward_bound`/`rim_runner` 188, `fuel_fumes_arrival` 192, `clean_getaway` 194, `silver_tongue`/`toll_paid` 195, `road_regular`/`debt_first_payment` 199, and `first_manifest`/`first_delivery`/`first_jump`/`beacon_keeper` 200/200. That spread is **recorded, not tuned away** — per the plan's own risk note, a 92/200 deed is evidence for T-1603/T-1603a, not a licence to edit content.

**ERA SWEEP EVIDENCE — seeds 1..200, horizon 400 days**, driver `support/era-sweep.ts#runSeed` (the same module the committed test imports, so the evidence and the assertion exercise identical code). **Started 6/6 and fired 6/6.** Per defId, started/fired out of 200: `blockade` 196/196, `fuel_crisis` 195/194, `patrol_crackdown` 194/194, `plague` 193/193, `dilithium_rush` 191/191, `famine` 188/188. 158 of 200 seeds are individually total on both unions — every era event is comfortably reachable and none is a long pole.

**Committed proofs (pinned, never hunting).** New `packages/sim/src/__tests__/deed-coverage.test.ts` carries both play-level acceptances off **one** pair of drives (a shared `beforeAll`, so the Conqueror proof does not pay for a second campaign): (1) *deed union* — target set **derived from `DEEDS`**, never hand-listed, so a deed added later with no coverage reds this test; pinned `PINNED_SEEDS = [2, 3]` at 300 days, each asserted **individually total** (`toEqual([])` on the missing list, not a `>= n` band); plus an honesty guard that `rankForDeedCount(earned.length) === registry.renownRank`, which would disagree if anything had poked a rank. (2) *CONQUEROR through play* — **T-1308's deferral is discharged**: seed 2 ends at `CONQUEROR` with `earned.length >= RENOWN_DEED_THRESHOLDS.CONQUEROR` (imported, never a literal 30, so T-1603's rescale moves the assertion with it), and the crossing is proven to have *happened during play* by a real `RenownRankUp{newRank:'CONQUEROR'}` in the event log (measured **day 102**) whose `previousRank` is below the capstone — plus **the reader proof** (Standing Constraint #7 / T-1308's reader (a)): a `WireEntry` carrying `RENOWN_RANKS.CONQUEROR.citation` **verbatim from content**, on the same day. Until now that engine branch had only ever been proven from a hand-constructed state in `engine/__tests__/deeds.test.ts`; this is the first proof it fires from a real career. T-1308's reader (b), the Nemesis-crossing stake gate, is **deliberately not asserted** — it stays T-1505b's and unstubbed. Measured pins: seed 2 — CONQUEROR day 102, last deed `slipped_the_scan` day 286; seed 3 — CONQUEROR day 57, last deed `mercy_runner` day 282. Horizon 300 not 500 (500 changes no outcome on either seed at ~3x the cost). Seed 1 is 43/44 even at 500 days, which is why it is not pinned.

**`era-storylet-coverage.test.ts` de-hunted.** Its inline `for (seed = 1; seed <= 30) { if (full) break; runSeed(seed, 400) }` is replaced by explicit `PINNED_SEEDS = [1, 11]` at a **200-day** horizon (measured: the seeds individually total at 200 days within 1..40 are 1, 11, 19, 21, 28, 29, 32; the two cheapest are pinned, two rather than one so a regression must break two independent careers). **Both assertions are byte-unchanged** and `TIE_INS` is still derived from `STORYLETS`. Beyond the 11.5s→5.9s saving, the hunt loop was actively harmful: on a *regression* the break never fires, so CI burned 30 × 400 days before reporting. The driver (`haulerDay`/`clearEncounter`/`runSeed`/`Sighting`) was extracted verbatim to **new `packages/sim/src/__tests__/support/era-sweep.ts`** — a pure move, on the `campaign-drivers.ts`/`deed-hunter.ts` precedent (a non-`*.test.ts` support module vitest never collects) — precisely so the `.scratch/` 200-seed script and the committed test run the same code; a pin backed by evidence from a *different* driver is not backed at all.

**`support/deed-hunter.ts` hardened, with zero behaviour change.** Header rewritten to name its real consumer (the two `it`s in `deed-coverage.test.ts`, not the nonexistent file it used to point at) and to carry a loud **"the pinned seeds depend on this file, byte for byte"** warning. Added a **deed-id drift guard**: content's `DeedId` is `export type DeedId = string`, so the ~17 ids inside `need(...)` were unchecked literals — a rename would silently stop the policy steering for that deed. They are now listed once as exported `HUNTER_TARGET_DEED_IDS`, with `need` typed to the derived union (a typo or stale rename is a **compile** error), and `deed-coverage.test.ts` asserts every entry is a real authored deed (catching a content-side rename). Values are identical to the previous inline literals; **verified by re-running the per-deed instrumented drive after the edit — seeds 2 and 3 reproduce every day number above exactly**, so the action stream is unmoved. `campaign-reach.test.ts`'s T-1504a NOTE is rewritten (comment-only; no seed, horizon or assertion touched) to record the discharge and point at the new file rather than re-running a second ~9s Conqueror campaign. `content/deeds.ts` is **comment-only**: the `RENOWN_DEED_THRESHOLDS.CONQUEROR` block now states the delivered fact (file, pinned seed, day 102 crossing, day 286 last deed) instead of forward-referencing this task, and the T-1308 capstone block's reader (a) clause is updated to "proven from play"; **`RENOWN_DEED_THRESHOLDS` values are untouched — that rescale is T-1603's.**

**Runtime.** Baseline `npx vitest run --root packages/sim`: **107.72s**, 12 files / 82 tests, with `era-storylet-coverage` at 21.4s under load. After: **110.57s**, 13 files / 85 tests — **+2.85s, far inside the <60s cap**. The new `deed-coverage.test.ts` costs 31.8s of its own but slots into the fork pool beside a 108s longest file (`campaign-policies`), so wall-clock growth is near zero; the era pin clawed back ~9.4s (21.4s → 12.0s), which is most of what the new file's scheduling cost. **No `GameState` field, event, or flag was added, so there is no save migration and no round-trip test — do not look for one** (same precedent T-1504c set). **Engine purity is trivially preserved:** no engine source is touched; all randomness continues through `SeededRng` via `driveCompetentCampaign`. **Player reachability (Constraint #6):** no new feature is added, so no new surface is owed — the Registry and rank citation are already player-visible via `deedRegistry()`/`rankCitation` in `ui/format.ts` → `registry-rank-citation` in `RecordsOverlay`, asserted at two ranks through the real cockpit by `ui/e2e/derule.spec.ts` (T-1504c). Gate: `npx tsc -b`, `npm run lint`, `npm run format:check`, `npm test` (engine 572 + sim 85) and `npm run test:e2e -w @spacerquest/ui` (67) all green. Scope boundaries: the `RENOWN_DEED_THRESHOLDS` rescale is T-1603's; the 500-seed fleet sweep is T-1603a's (which depends on this task and can reuse both `.scratch/` sweep shapes and the extracted drivers); T-1308's reader (b) stays T-1505b's.
Orchestration: graphify=none — no `graphify-out/graph.json` in the repo root. · attempts=1/4.

### T-1505a · Nemesis fragments & decode paths — `status: DONE` · `coder: fable` · `after: T-1403, T-1502`
The career mystery's collectible layer: 7 net-new fragments (`frag-nemesis-06..12`, joining the existing 01..05) authored across ≥3 acquisition modes (derelict logs, Sage decodings, NPC-held pieces), each with its decode path, plus the decoded-lore index text. No endgame content in this task — the crossing is T-1505b, the ending T-1505c.
**Accept:** 12 fragments total load and validate; each of the ≥3 acquisition modes yields ≥1 fragment in a sim test; all 12 have working decode paths; the decoded-lore index renders in the Nemesis File pane for decoded fragments; full gate green.
**Delivered (2026-07-26):** Fragments 06–12 are authored in `nemesis.ts` (now wrapped in a new `defineSignalFragments` validator, `content/nemesisValidation.ts`, mirroring the `defineStorylets`/`defineDeeds` load-time-throw shape) across three real acquisition modes: derelict/beacon loot-pool growth (06/07 added to `DERELICT_FRAGMENT_POOL`, 08 to `BEACON_FRAGMENT_POOL`), two NPC-held grants (`npc.rust-bucket.scrap-sliver` at Fomalhaut-2, `npc.void-whisper.psalm-shard` at Mira-9, both requirement-free so all twelve stay obtainable by a broke captain), and two Sage-archive grants gated on `nemesis.minFragments` (`sage.mizar.archive` at 1, `sage.mizar.final-line` at an interim 6 — T-1603 owns the real threshold). All seven get `sage.mizar.decode-06..12` decode storylets modelled on the existing 02–05 block, and every granting choice sets `fragmentSource` explicitly since engine `applyEffects` defaults it to `'wise-one'` — this is also what makes the previously-dead `'sage'`/`'npc'` `FragmentSource` literals load-bearing for the first time. New `sim/__tests__/nemesis-fragments.test.ts` drives each of the three modes through legal engine actions only (pinned seeds, no state pokes) and asserts the granted source; a new `ui/e2e/nemesis-file.spec.ts` proves the decoded-lore index through the real Nemesis File pane. Scope boundary: no destination gate, stake, or ending content — nothing here touches `nemesis.crossing.*`; the crossing is T-1505b's and the ending T-1505c's, both left untouched.
Orchestration: graphify=none — no graphify-out/graph.json in the repo root · attempts=1/4.

### T-1505b · The crossing & the stake — `status: DONE` · `coder: fable` · `after: T-1505a`
The endgame chain: the crossing lifts T-1101's NEMESIS destination gate via its `nemesis.crossing.unlocked` flag and requires the PRD's stake (ship + bank commitment); Conqueror interacts per T-1308's reader. Andromeda itself stays sealed for the expansion. Ending screen is T-1505c.
**Accept:** crossing completable in a scripted sim only with the full decoded set and the stake paid; the NEMESIS destination is travelable only after the flag (gate lift asserted both ways); declining or failing the stake leaves state consistent and re-attemptable; the Conqueror interaction asserted per T-1308's reader; full gate green.
**Delivered (2026-07-26):** The endgame chain lands end to end. Content (`nemesis.ts`) authors the crossing's data — `CROSSING_REQUIRED_RANK` (CONQUEROR, discharging T-1308's deliberately-unstubbed reader (b)), `CROSSING_DECODED_REQUIREMENT` (derived from `ALL_FRAGMENT_IDS.length`, never a literal 12), the `CROSSING_STAKE_MIN_CREDITS` floor, `NEMESIS_CROSSING_DC`, and the two authored `CROSSING_WIRE` lines — while the engine (`nemesis.ts`) owns the one authoritative refusal ladder as a pure `quoteCrossingStake` read shared byte-for-byte with the mutating `commitCrossingStake`, the sole setter of `nemesis.crossing.unlocked`; a new `nemesis.crossing.the-stake` storylet at Mizar-9 (`content/storylets.ts`) offers the choice only once the full set is decoded and the rank is met, repeats daily so a decline or refusal is re-attemptable, and retires permanently once the flag is set. `travelDc` gains an optional destination parameter so the crossing jump prices at the content DC instead of the unreachable distance-scaled one (both `travelPreview` and `resolveTravel` pass it so they can't disagree), `resolveTravel` skips the encounter roll on the NEMESIS leg (deterministic endgame, no golden ever reached system 28 before this task) and files the arrival event + wire line, and `day.ts`/`protocol.ts`/`format.ts` narrow the destination-gate lift to NEMESIS_SYSTEM_ID only so Andromeda and MALIGNA stay sealed for the expansion even post-stake (asserted both ways in all three layers). The UI (`App.tsx`/`format.ts`/`theme.css`) adds a Records → Nemesis "THE CROSSING" block (hidden until a fragment is decoded, then locked/committed/crossed) and a distinct starmap glyph for the newly-revealed system. New coverage: `engine/__tests__/crossing.test.ts` (365 lines, the refusal ladder both ways including the not-conqueror/conqueror T-1308 assertion), `sim/__tests__/nemesis-crossing.test.ts` (297 lines, scripted full-arc sim proof), and `ui/e2e/nemesis-crossing.spec.ts` (189 lines) through the real cockpit, plus updates to `day.test.ts`, `storylets.test.ts`, `systems.test.ts`, `protocol.test.ts` and `deed-coverage.test.ts` for the narrowed gate and new event/trigger shapes. No new `GameState` field — the crossing rides the existing `flags` map, so no save migration. Scope boundary: the ending screen and the acquisition-funnel UI proof are T-1505c's, left untouched; the `NEMESIS_CROSSING_DC`/`CROSSING_STAKE_MIN_CREDITS` numbers are INTERIM, T-1603 owns the rescale.
Orchestration: graphify=none — no `graphify-out/graph.json` in the repo root (checked; the path does not exist). · attempts=1/4.

### T-1505c · Nemesis ending & funnel proof — `status: DONE` · `coder: fable` · `after: T-1505b`
The v1 ending screen and the arc's integration proof: full arc in a scripted long sim, and the acquisition funnel proven once through the real UI.
**Accept:** full arc (12 fragments → decode → crossing → ending) completable in a scripted long sim; the acquisition funnel proven through the real UI (Playwright: explore→fragment→Sage decode); ending reachable and returns to menu cleanly; full gate green.
**Delivered (2026-07-26):** The career's terminus lands as a DERIVED state, not a new field: engine `careerEnded` (nemesis.ts) reads true exactly when `player.currentSystemId === NEMESIS_SYSTEM_ID`, so there is no save migration. A new terminal guard in `applyPlayerAction` (day.ts), ordered above the T-1101 destination gate, refuses every blockable verb from the far side with a typed `ActionBlocked{reason:'career-ended'}` (new enum member threaded through `schema.ts` and `types.ts`), and the sim's `legalActions` (protocol.ts) advertises nothing once `careerEnded` holds so a headless driver stops for the same reason a player does. Engine `careerEpilogue` derives the ending screen's whole payload — rank, deed count, fragment counts, stake paid, systems charted, succession count — purely off the ended state, folded with the authored epilogue text in content's new `CROSSING_ENDING` (nemesis.ts) and the arrival wire line `CROSSING_WIRE.crossed`. The UI (`endingScreen` in format.ts, `EndingScreen` in App.tsx) replaces the cockpit outright once the career is over rather than covering it, with a single control that starts a fresh career via the existing `newGame` store action. New coverage: `sim/__tests__/nemesis-arc.test.ts` (396 lines) drives the full scripted long sim — 12 fragments through all acquisition modes, decode, crossing, and arrival — and asserts the epilogue; `ui/e2e/nemesis-funnel.spec.ts` (139 lines) proves the acquisition funnel once through the real UI (explore → fragment → Sage decode); `ui/e2e/nemesis-ending.spec.ts` (164 lines) proves the ending screen renders and its return control lands back on a clean day-1 cockpit; `engine/__tests__/crossing.test.ts`, `day.test.ts`, `save.test.ts` (round-trip of an ended career through createSave/loadSave), and `sim/__tests__/protocol.test.ts`/`nemesis-crossing.test.ts` gained matching unit coverage for the terminal guard and stop signal. Scope boundary: no content past the threshold — Andromeda and MALIGNA stay sealed for the expansion, and the ending prose (`CROSSING_ENDING`) narrates only the crossing itself, promising nothing about the far side.
Orchestration: graphify=none — no `graphify-out/graph.json` exists in the repo root (checked; path absent). · attempts=1/4.

---

## M16 — Hardening & balance

### T-1601a · Policy fleet v3: veterans learn the new verbs — `status: DONE` · `coder: opus` · `after: —`
Upgrade the three existing policies: the explorer explores and decodes, the trader runs the rim and borrows under duress, the fighter uses its now-real equipment. The stats report gains the metrics these behaviors produce: loan usage and the T-1004 `fuelStarvationDays`. This task also owns the lending rate/term band interim constants (`content/lending.ts`) — the trader's borrowing exercises them.
**Accept:** each upgraded policy's 300-day report renders with its new metrics nonzero where applicable (loan usage for the trader, fragments for the explorer, equipment use for the fighter); trader clears Tour One within the interim band (≥50% of 50 seeds); no upgraded policy triggers a poverty-trap (criterion scoped to the competent policies, per the T-1005 errata); full gate green.
**Delivered (2026-07-26):** All three veteran policies (`packages/sim/src/index.ts`) learn the new verbs. `traderPolicy` gains Penny Wise borrow/repay (`planLoanBorrow`/`planLoanRepay`): a shortfall-sized advance under three duress shapes (fuel, repair, marker) plus an honest "working capital" case so the lending band ships exercised, protected repayment ahead of the Guild marker, and a rim-run preference once the marker clears. `explorerPolicy` gains a decode leg to the Sage of Mizar-9 (system 18) — a gated deadhead trip past day 30 above a 10,000-credit reserve, plus preferring a fundable run that already ends there — and the T-1104 rim-reachability relaxation ported from the trader so a rim-stranded explorer no longer waits forever. `fighterPolicy` reorders special-equipment purchase ahead of the hull-tier upgrade (AUTO_REPAIR prices off current hull strength: 1,000 credits pre-refit vs 20,000 after) and widens its exclusion ladder to mirror the engine's cloaker/titanium-hull conflicts. The stats report (`CampaignStatsReport`) gains three new derived, unpersisted blocks — `loanUsage`, `fragments`, `equipmentUse` — folded from the typed event stream by a new `accumulateMetricEvents`, plus a per-day `fuelStarved` series backing the existing `fuelStarvationDays` scalar. This task also ratifies (not moves) the `content/lending.ts` interim band: seeds 1..8 × 300 days show every seed borrowing, accruing, and clearing in full at the desk, so the constants ship unchanged with their INTERIM ownership re-pointed from the since-split T-1601 to T-1603b. Scope boundary: the lending/economy numeric rescale itself, the smuggler/gambler policies, and the guild/dice-hook constants stay T-1601b/T-1601c/T-1603's, untouched here.
Orchestration: graphify=none — no `graphify-out/graph.json` in the repo root (checked; the path does not exist). · attempts=1/4.

### T-1601b · Policy fleet v3: smuggler & gambler — `status: DONE` · `coder: opus` · `after: T-1601a`
Add the two net-new policies: smuggler (contraband runs, patrol scans, fence flow) and gambler (Hangout visits, Spacer's Dare wagers). The stats report gains their signature metrics: scan outcomes and Hangout EV.
**Accept:** both new policies' 300-day reports render with scan outcomes (smuggler) and Hangout EV (gambler) nonzero; neither triggers a poverty-trap; full gate green.
**Delivered (2026-07-26):** Two net-new `SimPolicy`s land in `packages/sim/src/index.ts`: `smugglerPolicy`, built around the contraband pillar (PRD §7.2/§7.5) — it deadheads to a rim port with `allowsContraband` when the board offers nothing fundable, signs and delivers type-10 contracts, takes sealed pods via a new `chooseSmugglerStoryletAction` picker (matched by choice ID, never ordinal, so content reordering can't silently flip it to the declining branch), and sells to Smuggler Ray; and `gamblerPolicy`, which plays the Spacers Hangout tables (Spacer's Dare) alongside its own income routing. The stats report (`CampaignStatsReport`) gains two new derived, unpersisted blocks — `SmugglingStats` (contract/delivery counts, scan/caught/evaded, fines, confiscations, pod/fence beats, exposure-day counters) and `HangoutPlayStats` (visits, dares, win/loss, wagered, net credits, `expectedValuePerDare`, social beats, and a `failedVisits` counter that proves the policy's guards mirror the engine's) — both folded from the typed event stream by an extended `accumulateMetricEvents`, now taking a single `CampaignMetricAccumulator` rather than a growing positional-arg list. `POLICY_NAMES` moved to `as const satisfies readonly SimPolicyName[]` so a policy name added to the union but forgotten (or misspelled) in the CLI list is a compile error. New coverage in `packages/sim/src/__tests__/campaign-smuggler-gambler.test.ts` (232 lines), sibling to `campaign-policies.test.ts` so vitest's fork pool runs them in parallel; the file's header records a 20-seed × 300-day sweep provenance (run in `.scratch/`, scripts not committed) for both policies, including the one seed that strands the smuggler early and the shared `planPacifistCombat` zero-income streak the gambler inherits. Scope boundary: this task adds only the two policies and their metrics — the lending/economy numeric rescale, the guild constants, and the dice-progression hook stay T-1601c/T-1603's, untouched here.
Orchestration: graphify=none — no `graphify-out/graph.json` in the repo root (checked; the path does not exist). · attempts=1/4.

### T-1601c · Deferred interim constants: guild & dice hook — `status: DONE` · `coder: opus` · `after: T-1601b`
The remaining constants deferred to old T-1601 by name: the guild constants (`content/guild.ts`) get their interim band, and the dice-progression extensibility hook lands (a future die-granting equipment module can add dice without touching the dawn-roll core, per T-1306's deferral). Small engine/content task — no new policy behavior.
**Accept:** guild constants carry INTERIM markers naming T-1603 as owner; the dice hook is exercised by a unit test granting a die through the extensibility point (no gameplay module shipped); deferral comments at the named sites updated; full gate green.
**Delivered (2026-07-26):** Two independent deliverables, zero balance numbers moved. **(A) Truth pass on the stale T-1601 ownership markers.** `content/guild.ts` now carries the ratified `lending.ts` idiom — `INTERIM (T-1603)`, canonical-values owner **T-1603b** (the economy/pacing pass that names "lending and guild bands" explicitly) — plus a T-1601c RATIFICATION paragraph stating the band ships UNCHANGED and why each value is a safe interim: 0.02/dusk compounds on a non-blocking ledger that never touches credits; the ×1.4 encounter and ×0.85 manifest constants are quoted at severity 1 and scaled by it; and the clamp band ([0.5, 2] around neutral 1 at 0.2/point) means the six pressure flags span score −3…+3 → 0.4 (clamped up to 0.5) … 1.6, so reachable severity never touches MAX. Evidence is the existing green coverage (`guild-pressure.test.ts`, 6 tests, pins all three readers plus standing→magnitude monotonicity; `tour-one-resolution.test.ts` covers both day-30 branches) — no new sweep, since fleet-wide measurement is T-1603a's by name. All seven per-constant `Interim (T-1601).` markers retargeted to `(T-1603b)`. The same stale "OWNED BY the T-1601 rebalance" defect in `content/crew.ts` and `content/ports.ts` (the only other two files carrying it, and `crew.ts` is a named site of the dice deferral) was closed in the same pass. **(B) The dice-progression extensibility point is now real rather than promised.** `dice.ts`'s "a future dice module would be a one-line change" prose was untestable — `dawnDiceModifiers(crew)` read crew and nothing else. It now takes a second, defaulted `equipment: readonly DiceBenefit[]` leg produced by the new `equipmentDiceBenefits(ship, table = EQUIPMENT_DICE_BENEFITS)`, and crew and equipment provably share ONE fold (the per-benefit switch is a single `applyBenefit` closure), so `MAX_EXTRA_DICE`/`MAX_DAWN_HAND_SIZE` clamp the COMBINED extra count, floors take the MAX across sources, and re-roll charges sum. The data side is `content/crew.ts` `EQUIPMENT_DICE_BENEFITS`, a `SpecialEquipmentContentId`-keyed table (derived in `upgrades.ts` from the shipped `SPECIAL_EQUIPMENT` literal, so a typo'd key is a compile error, while the widened `SPECIAL_EQUIPMENT` export keeps every existing consumer's type unchanged) that **SHIPS EMPTY** — no gameplay module and no new `SpecialEquipmentId` — so the live path is byte-identical. Wired at all three live call sites (`day.ts` startDay, `actions/crew.ts` resolveReroll, `ui/format.ts` dawnHandModifiers) so engine and HandDock badges can never disagree; shipyard's private `alreadyInstalled` mapping was extracted to `components.ts` `hasSpecialEquipment` (exhaustive switch, `default: false` — dropping shipyard's "everything else is TRANS_WARP" fallthrough) so the purchase guard and the dice lookup share one definition of "fitted". Six new tests in `engine/src/__tests__/dice.test.ts` grant a die through the extension point (the accept criterion), through the full ship→table→dealt-hand chain (`rollDawnHand` deals 6), prove an unfitted module grants nothing, pin the combined clamps and the floor/reroll folds against the imported constants, and prove neutrality: the shipped table is empty and a ship with EVERY module fitted deep-equals the crew-only aggregation. **No `GameState` field was added** — the grant is derived from the fitted-equipment flags already on `ShipState` and never stored — so there is deliberately no migration and no new round-trip test; the two golden serialized-STATE hashes and the sim determinism suites pass unchanged, which is the behavior-neutrality proof. Gate: `tsc -b`, `npm run lint`, `npm run format:check`, `npm test` (engine 636, sim 102) and, because `format.ts` is in scope, `npm run test:e2e -w @spacerquest/ui` (71 passed) all green.
Orchestration: graphify=none — no `graphify-out/graph.json` in the repo root (checked; the path does not exist). · attempts=1/4.

### T-1602a · Tour One career spec — `status: DONE` · `coder: opus` · `after: T-1407`
Playwright: complete Tour One start-to-resolution through the real UI (per global test-intent rules — every step a player keystroke/click, zero engine shortcuts), both resolution branches, under the CI gate that actually executes it (T-1001). Audit note: Tour One is exercised piecemeal today (`onboarding.spec.ts` reaches the day-31 resolution) but no single start-to-resolution career spec exists. Death-and-legacy and the flake gate are T-1602b.
**Accept:** both branch tests green locally and in CI's Playwright job (post-push, per the CI-evidence rule); run report artifact (screens visited, days elapsed); full gate green.
**Delivered (2026-07-26):** `packages/ui/e2e/tour-one-career.spec.ts` plays Tour One day 1 through the day-31 resolution ceremony entirely through the real cockpit — no `createInitialState`, no store `page.evaluate`, no localStorage seeding, not even a type import from the engine — via a new driver, `packages/ui/e2e/support/career.ts` (885 lines): `startCareer` (New game → seeded boot), `sightseeingPass` (a free, rng-free tour of every screen on day 1's Sun-3 Hangout stop), `playDay` (one full day's contract-pick/fuel/travel/combat-stance/marker-pay decisions, returning the new day number so the spec can assert exactly one day advances per call), `acknowledgeResolution`, `expectPlayableCockpit`, and `emitReport` (writes the run-report artifact CI now uploads). Two tests share one driver and one pinned seed (21, swept 1..40 against this exact driver — 11/40 qualified at ≥25,000cr day-30 bank; 21 chosen over the higher-banking 29 because it exercises six combat interceptions instead of one) and differ by exactly one player decision — paying the Guild marker through the Port Ledger on day 30 — covering both resolution branches (`cleared`: veteran unlocked, deed titled, debt chip gone; `unpaid`: resolution files a consequence, veteran locked, debt chip persists at the full marker) plus the T-1301 TOUR_ONE→VETERAN era flip, asserted for the first time via a new `campaign-era` testid. Four `data-*` attributes (`data-fuel`/`data-can-run` on the combat fuel readout; `data-destination-id`/`data-payment`/`data-fuel-cost`/`data-dc`/`data-pods`/`data-contraband` on manifest contract rows; the same route pair on the active-contract tracker) were added to `packages/ui/src/App.tsx` so the driver reads engine-sourced numbers structurally instead of parsing prose — every value is the same number already rendered, so the UI gains no new derived rule. CI (`.github/workflows/ci.yml`) uploads the run report (`packages/ui/test-results/tour-one/`) as an `always()` artifact with `if-no-files-found: error`, so a failing career still yields its day/encounter log and an empty upload cannot pass silently. Scope boundary: death-and-legacy and the 20-run flake-rate measurement are T-1602b's, deliberately untouched here; the existing `onboarding.spec.ts` day-30 fixture boot (`bootDay30`) is left as-is since it proves a different thing (T-311) than a full from-day-1 run.
Orchestration: graphify=none — no `graphify-out/graph.json` in the repo root (checked; the path does not exist). · attempts=1/4.

### T-1602b · Death, legacy & flake gate — `status: DONE` · `coder: opus` · `after: T-1602a`
The death-and-legacy career run through the real UI, plus the stability proof for the whole Tour One E2E suite: flake rate measured over repeated CI runs.
**Accept:** death-and-legacy spec green in CI; flake rate <2% over 20 CI runs of the full Tour One E2E suite (push-dependent — recorded in the Delivered note per the CI-evidence rule); full gate green.
**Delivered (2026-07-26):** Two deliverables. **(A) Death and legacy, through the real cockpit.** `packages/ui/e2e/tour-one-death.spec.ts` (550 lines) plays both of the game's death paths — the combat killing blow and the dusk life-support failure — from the boot screen with every action a real click: the contract signed off the manifest, the jump plotted on the starmap, the fight fought round by round, the death arriving from the engine's own dice. The injected save sets the SCENARIO ONLY (the sanctioned `nemesis-file.spec.ts`/`onboarding.spec.ts` fixture pattern): a day-12 spacer at rim port Antares-5 with a tier-1 hull worn to condition 1, an 8,401cr bank (odd on purpose, so the inheritance assertion proves FLOOR division and not merely "about half"), a fragment in the Nemesis file, a grudge, League standing 4 and the 25,000cr Guild marker — every grantable piece written through the engine's own movers (`grantFragment`, `applyDisposition`, `applyReputation`). Seeds pinned from a 200-seed headless sweep replaying this spec's exact decision rules (`.scratch/`, not committed): **192**, where Lucky Seven — a NAMED tier-2 hunter — takes the ship on the second fight round (chosen over the six 1-round kills because two rounds actually exercise the round loop, and over the anonymous killers because a named interceptor also proves the notice resolves an id to its authored name), and **3** for the dusk GRIT survival failure (23 of the first 60 seeds fail it). A from-day-1 death was rejected with arithmetic recorded in the spec header: ~9 hull-targeted hits against a uniform 1-in-8 component pick is 100+ combat rounds, against a day that yields ~6. The UI this needed did not exist — until now a player who lost their ship watched the combat overlay unmount, their credits halve and their position teleport with no in-the-moment explanation at all, the only trace an obituary buried in the Wire log. `format.ts` gains `successionSummary`, pure translation of the engine's own typed batch (`ShipLost` + `LegacySuccession` + `TradeEvent{forfeit-cargo}` + the obituary `WireEntry`): the halved bank is the engine's `inheritedCredits`, the obituary is the engine's sentence verbatim, and the killer's display name is a content lookup (`ANONYMOUS_INTERCEPTORS`/`NPC_PROFILES`), never a rule. `store.ts` gains a client-only `succession` field set from BOTH death paths (`combat()` and `endDay()`) plus `dismissSuccession`; `App.tsx` renders `SuccessionNotice` mounted OUTSIDE the combat overlay, because a dusk life-support death has no encounter on screen to hang off and the combat kill nulls the encounter before render — hanging it off the overlay would leave half the deaths in the game invisible. In the same pass `combatAftermathSummary`'s `ShipLost`/`LegacySuccession` branches were deleted as provably unreachable copy (`combat.ts` returns after `ShipLost` without emitting `EncounterResolved`, so the function has already returned null). The DURABLE reader is new too: `deedRegistry` exposes `successionCount`, a verbatim read of the persisted `player.legacy.successionCount` (T-108), rendered as the Registry's `registry-successions` row above zero only — asserted after a reload, which is what proves the counter is state and not a modal. `ComponentRow` gains `data-strength`/`data-condition`, structural pass-throughs of two numbers the row already renders (condition as five pips, unreadable structurally), read by the spec to prove the successor inherits an exact junker. **No `GameState` field was added** — `succession` is client presentation meta-state like `combatAftermath`/`patrolScan`, and the persisted half already ships in `LegacyStateSchema` — so there is deliberately no migration and no new round-trip test. **(B) The flake gate, and the round-2 correction that made it real.** The pipeline: the `@tour-one` tag on the four Tour One tests (2 career + 2 death); a `FLAKE_RUN=1` mode in `playwright.config.ts` (`retries: 1` — the only setting under which Playwright's `flaky` and `unexpected` mean what the gate says they mean — plus a JSON reporter at `FLAKE_REPORT`); `e2e/support/flake.ts`, the PURE aggregator (no `fs`, no `process`, no clock) that defines the metric as (flaky + unexpected) / non-skipped results over the tagged scope, keeps `skipped` out of the denominator, and fails the gate on ANY `unexpected` at any rate because a test that failed every attempt is a bug and must not be laundered through a percentage; and `e2e/flake-rate.spec.ts`, six unit tests pinning that arithmetic — including the sigil case, since the JSON reporter writes `tour-one` where the spec authors `@tour-one` and a mismatch would collapse the denominator to zero and print a reassuring 0.00% over nothing. **Round-2 diagnosis: the measurement could never be taken.** Round 1 shipped it as a `workflow_dispatch`-only matrix, and GitHub dispatches only workflows already present on the DEFAULT BRANCH — so a workflow added by the very commit whose acceptance needs it answers HTTP 404 ("workflow not found on the default branch"), and once merged nothing would have started it either, because dispatch-only means a human has to remember. Three fixes. (1) `.github/workflows/e2e-flake.yml` now also fires on `push` to main/rimward-redesign, scoped by `paths` to the specs, the config, the UI manifest that pins Playwright, and the workflow itself: round 1's cost argument survives (20× the suite is a real bill) while the stability question re-opens exactly when the measured thing changes. `RUNS` is resolved once at workflow level as `${{ inputs.runs || '20' }}`, because `inputs` is null on a push event and `[ 1 -le "" ]` is a shell error rather than a comparison — as written it would have failed all twenty shards of every pushed measurement — and the shard gate now validates the value instead of dying unreadably on junk. (2) The CI-evidence rule's "local equivalent of what CI runs satisfies review" had nothing to point at, since `flake-cli.ts` only aggregates reports the matrix already produced. `e2e/support/flake-measure.ts` (`npm run flake:measure -w @spacerquest/ui`) is that equivalent: N sequential runs under the identical `FLAKE_RUN`/`FLAKE_REPORT` env, each booting its OWN preview server exactly as a fresh matrix shard does (a shared warm server would measure away the post-`goto` boot window where fix-round-1's two observed stalls happened), a failing run treated as the datum rather than a stop (mirroring `fail-fast: false` + `continue-on-error`), ending in the SAME aggregation and gate via a shared `reportDirectory` — `e2e/support/flake-io.ts`, now the one place that touches the filesystem, extracted precisely so CI and local cannot drift. (3) A real bug the local runner exposed on its second run and CI structurally could not: the per-run reports were written to `test-results/flake/`, which Playwright ERASES at the start of every run. Harmless with one run per matrix shard; fatal on one machine, where run 2 destroyed run 1's report and a 20-run measurement would have ended holding a single file. Reports now live in `packages/ui/flake-results/` (gitignored) on both sides, with the reason recorded at all three sites. **The measurement (local, per the CI-evidence rule):** `npm run flake:measure -w @spacerquest/ui`, 20 full-suite runs of 81 tests in 940s — **1,620 results, of which 80 are `@tour-one`: 0 flaky, 0 unexpected, 0 skipped, a 0.00% flake rate against the <2% bar, gate PASS** (exit 0), and the whole-suite informational number is 0.00% as well: no test in the suite misbehaved in any of the twenty runs. With 4 tagged tests × 20 runs = 80 results the resolution is coarse and worth stating plainly — a single flake would have read 1.25% and passed, two would have read 2.50% and failed — so this is a clean sample, not a wide margin. The push that lands this commit fires the same twenty-shard matrix over the same code and tees the same table into the job step summary; the death spec itself additionally rides the ordinary `ci.yml` Playwright job, which runs the whole `e2e/` directory. Scope boundary: no balance number moved, and T-1603c owns the re-pin of both death seeds under the rebalance-fallout rule if it moves enemy damage, the component spread, hull condition, the life-support DC or the Auto-Repair interaction — recorded as a FALLOUT OWNER note in the spec header, not left to memory.
Orchestration: graphify=none — no `graphify-out/graph.json` in the repo root (checked; the path does not exist). · attempts=3/4.

**Note:** T-1603a–c below are collectively the "T-1603" that earlier tasks, INTERIM
markers, and the rebalance-fallout rule refer to as the canonical-values owner.

### T-1603a · Balance baseline: 500-seed fleet sweep — `status: DONE` · `coder: fable` · `after: T-1601c, T-1504d`
Measurement only, no tuning. Run the policy fleet across 500 seeds against the game whose numbers now bind — fuel scarcity (T-1102), foundation-anchored encounters (T-1103), load-bearing components (T-1205), margin scaling (T-1202) — and record baseline distributions for every PRD target: Tour One clear day, route EVs, combat EV by tier parity, death rate, deed pacing. The sweep runs as a committed, re-runnable script (not a CI test).
**Accept:** baseline memo in `docs/balance/` with a distribution per PRD target; the sweep script committed and documented; zero game-behavior diff (`npm test` untouched-green); full gate green.
**Delivered (2026-07-26):** A committed, re-runnable sweep (`packages/sim/src/balance/sweep.ts`, sharded and mergeable, plus the pure `packages/sim/src/balance/aggregate.ts` that defines every metric — nearest-rank quantiles, tier-parity sign convention, engine-anchored repair valuation, combat EV, route EV, death rate — pinned by 417 lines of fixture tests in `packages/sim/src/__tests__/balance-sweep.test.ts`) plus a new `balance:sweep` npm script in `packages/sim/package.json`. Two arms were run: Tour One (500 seeds × 7 policies × 35 days, 3,500 runs) and veteran (100 seeds × 7 policies × 120 days, 700 runs) — two arms rather than one 300-day sweep because `runCampaign` is quadratic in days (Flag 6). The seven policies are `trader, fighter, explorer, veteran, smuggler, gambler` plus `greedy` as a naive control; `idle`/`random` are excluded from the default fleet as noise. Results are written to `docs/balance/baseline-tour-one.json`, `docs/balance/baseline-veteran.json`, and the full write-up `docs/balance/BASELINE-T-1603a.md` (distributions for all five PRD targets, method, provenance, and seven numbered flags handed forward to T-1603b/T-1603c — deed/renown pacing sized for a longer career than the game plays; combat has no payout at all so `combatEv ≤ 0` by construction; preparation pays off backwards in Tour One; the death rate is nonzero but tiny and the combat death path is structurally unreachable; the Auto-Repair/life-support interaction from T-1804 confirmed with evidence — `fighter` is the only policy with zero deaths in 12,000 veteran-arm days; the quadratic runtime; and `explorer` never paying the Guild marker). **Scope boundary: measurement only** — no constant, DC, or price moved; `npm test` is untouched-green; the game-behavior diff against the pre-task tree is zero, confirmed in the memo's Provenance section. Raw per-run rows (3,500 + 700 records) are gitignored under `.scratch/balance/` and are not a repo artifact — only the merged aggregates are committed. Tuning against these baselines is T-1603b's and T-1603c's, next in the T-1603 sequence.
Orchestration: graphify=none — no `graphify-out/graph.json` exists in the repo root (checked; the path is absent). · attempts=1/4.

### T-1603b · Economy & pacing tuning — `status: TODO` · `coder: fable` · `after: T-1603a`
Tune the economic constants against the baseline: canonical values for danger tiers 2/4/5 (`content/systems.ts`), port pricing/income (`content/ports.ts`), lending and guild bands, hangout wagers. Targets: Tour One clearable by competent play in 25–30 days (not 10, not never); no dominant route (era churn working); deed pacing.
**Accept:** median trader debt-clear day in [22, 30]; no stable optimal route across the fleet; every touched INTERIM marker replaced with a canonical-value comment; broken tests fixed in the same commit (rebalance fallout rule); memo updated with before/after distributions; full gate green.

### T-1603c · Combat & survival tuning — `status: TODO` · `coder: fable` · `after: T-1603b`
Tune the combat/survival side: combat EV negative below tier parity without preparation (testable now that components matter), nonzero death rate (closing the audit's zero-deaths finding). Balance consideration flagged by the audit (T-1804): Auto-Repair regenerating before the life-support dusk gate makes the life-support death path unreachable whenever the module is fitted — evaluate whether an always-rescue module is too strong, and either retune or ratify with rationale.
**Accept:** combat EV negative below tier parity unprepared; nonzero death rate across 1,200 sim days; the Auto-Repair call recorded in the memo (retuned or explicitly ratified); memo finalized with before/after distributions; all prior tests still green; full gate green.

### T-1604a · UGT campaign run — `status: TODO` · `coder: fable` · `after: T-1602b`
Point UGT (sibling repo) at Rimward via the repaired adapter (T-1003). Run the autonomous playtest loop per the established memory protocol (no stopping per failure) and produce the triaged findings report. Fixes are T-1604b — this task changes no game code beyond what the adapter hookup itself requires.
**Accept:** ≥1,000 UGT actions logged; `ActionBlocked` UI/protocol event parity verified during the campaign; findings report with severity triage (HIGH/MED/LOW) committed to `docs/playtests/`; full gate green.

### T-1604b · UGT HIGH-finding fixes — `status: TODO` · `coder: fable` · `after: T-1604a`
Work the T-1604a report: every HIGH finding fixed on this list's pattern. If the HIGH list turns out large (>~5 findings), split further before starting — one fix-batch task per subsystem — rather than absorbing them all here.
**Accept:** every HIGH finding fixed with a regression test, or explicitly waived by the user with the waiver recorded in the report; report updated with resolutions; full gate green.

### T-1605a · Crash & corrupt-save recovery — `status: TODO` · `coder: opus` · `after: T-1602b`
Error boundaries with save-preserving recovery, and the corrupt-save UX (today a corrupt save silently falls back to a fresh career at `store.ts:256` — the player must be told).
**Accept:** forced crash recovers without save loss; corrupt-save path shows a visible notice instead of silently resetting (Playwright); full gate green.

### T-1605b · Poverty-trap invariant over adversarial states — `status: TODO` · `coder: opus` · `after: T-1602b`
Extend the anti-poverty-trap invariant over the new adversarial states: indebted-to-Penny-Wise, post-confiscation, zero-fuel-rim.
**Accept:** invariant property test in CI covering the three named adversarial states; full gate green.

### T-1605c · Performance pass — `status: TODO` · `coder: opus` · `after: T-1602b`
Long-career performance: 1,000-day event logs must stay loadable and responsive.
**Accept:** a 1,000-day save loads <2s (asserted in a committed test with the measurement method documented); no load-time regression for normal-size saves; full gate green.

---

## M17 — Ship it

### T-1701a · Electron shell & local saves — `status: TODO` · `coder: fable` · `after: T-1602b`
Electron wrapper per TECH-STACK lean: dev-mode shell, local save dir (migrating localStorage saves in), window management. Keep the web build working. Packaging and the updater stub are T-1701b.
**Accept:** dev-mode Electron runs Tour One start-of-career; saves land in OS app-data with the localStorage migration verified; web build unaffected (CI proves both); full gate green.

### T-1701b · Packaging & updater stub — `status: TODO` · `coder: fable` · `after: T-1701a`
Auto-updater stub plus mac+win packaging scripts for the T-1701a shell.
**Accept:** packaged app runs Tour One on macOS; win package builds; updater stub present and inert without a feed; CI proves web + both platform builds; full gate green.

### T-1702a · Steamworks core & achievements — `status: TODO` · `coder: fable` · `after: T-1701b`
steamworks.js (or equivalent) initialization with the graceful no-Steam fallback designed in from the start, plus achievements mirrored from the ≥30-Deed set including Conqueror. Cloud and rich presence are T-1702b.
**Accept:** achievements fire from deed events in the Steam dev sandbox; app runs identically without Steam present; full gate green.

### T-1702b · Steam Cloud & rich presence — `status: TODO` · `coder: fable` · `after: T-1702a`
Steam Cloud on the seed-carrying T-1002 envelope; rich presence (current system/day).
**Accept:** cloud round-trip verified in the dev sandbox; rich presence shows current system/day; the no-Steam fallback still clean for both features; full gate green.

### T-1703 · Demo build (Tour One) — `status: TODO` · `coder: opus` · `after: T-1702b`
Demo configuration: Tour One + 3 post-resolution days, veteran features teased-but-gated — the gate list includes Hangout progression, ports, and Conqueror content — demo-save carries into full game, distinct build flag and Steam depot config.
**Accept:** demo build produces the gate correctly (no veteran content reachable — Playwright proves it); save import works full-side; build size sane (<200MB).

### T-1704 · Release checklist — `status: TODO` · `coder: opus` · `after: T-1603c, T-1604b, T-1605a, T-1605b, T-1605c, T-1702b, T-1703`
Final sweep: store-page asset export list, credits (fonts/audio licenses), version stamping, README/press one-pager, tag `v1.0.0-rc1`.
**Accept:** checklist doc complete with every item checked or explicitly waived by the user; RC tag builds green from clean clone.

---

## Deliberately deferred (do not scope-creep into v1)
Async arena PvP (Season 1) · Andromeda region (expansion) · Roscoe upgrade path · gambling games beyond Spacer's Dare · ports-as-property beyond the single-stake mechanic · localization · controller support.
