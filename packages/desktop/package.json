{
  "name": "@spacerquest/desktop",
  "version": "1.0.0",
  "private": true,
  "description": "T-1701a · Electron shell: window management + an OS app-data save dir. Zero game rules.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -b",
    "dev": "npm run build && electron dist/main.js",
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.5",
    "electron": "^43.2.0",
    "typescript": "^5.9.3",
    "vitest": "^1.5.0"
  }
}
